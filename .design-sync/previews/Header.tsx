// Preview for Header — the Case-File marketing bar: logo mark + wordmark left,
// nav links + the gold "Join the beta" CTA right. No props (it owns its content
// from siteConfig), so the one canonical story is the whole bar. The column
// override renders it as the wide, short strip it is in the layout.
import { Header } from 'open-pro-next'

export function Canonical() {
  return <Header />
}
