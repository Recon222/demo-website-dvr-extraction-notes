// Preview for RoadmapTease — the sealed roadmap section (dashed cards, ROADMAP ·
// NOT IN BETA tags, NEXT NN kickers, deliberately muted — no dates, no promises).
// No props: the three teaser cards are the component's own canvas copy. One render.
import { RoadmapTease } from 'open-pro-next'

export function Canonical() {
  return <RoadmapTease />
}
