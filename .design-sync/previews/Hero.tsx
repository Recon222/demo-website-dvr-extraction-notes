// Preview for Hero — the Case-File split hero (credential chip, H1, sub, CTA row,
// cred strip ⟷ bracketed phone). No props: it owns its own headline (siteConfig
// .tagline), copy, and the phone frame's walkthrough loop. One canonical story is
// the whole component — a full-width page section with a single natural render.
import { Hero } from 'open-pro-next'

export function Canonical() {
  return <Hero />
}
