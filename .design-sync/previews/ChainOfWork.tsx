// Preview for ChainOfWork — the 4-column chain-of-work section (glowing halo dots,
// connector lines, STEP NN kickers, step copy + JetBrains chips). No props: the
// four steps are the component's own transcribed canvas copy. One natural render.
import { ChainOfWork } from 'open-pro-next'

export function Canonical() {
  return <ChainOfWork />
}
