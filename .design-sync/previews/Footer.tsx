// Preview for Footer — the Case-File footer: mini logo mark + copyright left,
// center link row, and the technical trust label right, over a hairline top
// border. No props (content is fixed / siteConfig), so one canonical story is
// the whole footer. The column override gives it the full-width strip it needs.
import { Footer } from 'open-pro-next'

export function Canonical() {
  return <Footer />
}
