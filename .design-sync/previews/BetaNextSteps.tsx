// Preview for BetaNextSteps — the three WHAT-HAPPENS-NEXT cards under the beta
// panels. No props; the copy and the cyan/gold/blue kicker axis are baked in.
import { BetaNextSteps } from 'open-pro-next'

export function NextSteps() {
  return <BetaNextSteps />
}
