// Preview for BetaCta — the gold beta-recruitment panel (EXHIBIT A tab, pitch copy,
// and the working email intake BetaForm with its compact consent row). No props:
// the panel owns its copy and mounts BetaForm from the bundle. One natural render.
import { BetaCta } from 'open-pro-next'

export function Canonical() {
  return <BetaCta />
}
