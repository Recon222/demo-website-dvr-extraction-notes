# U7.3 — OCR, copy rules, mono policy — implementation report

**Package:** U7.3 (M, phase U7, wave W3) — the LAST U7 package · **Branch:** `uiparity/u7.ocr` off `feat/uiparity-u7` @ `02bba18`
**Head:** `18f3e24` · 6 commits · **Seat:** `opus-implementer` (Opus 5) · **Written:** 2026-08-27
**Matrix rows:** A93, A94, B.6 rows 37, 71/72, 73, 79 · **Decisions:** D12, D13, D17, D19, D20
**Phone:** `extraction_case_notes_react_native_expo` @ `dd5551ec`, READ-ONLY.

---

## 1. Refutations first

Every claim in the brief, the plan and the matrix was opened at source. Seven disagree with
reality, and two of them change what the package could honestly do.

### R-1 — A94's premise is false: the phone declares TWO mono roles, and both are live

A94's Delta column states *"The phone has exactly **one** mono face and it is Share Tech Mono"*
and *"The demo's JetBrains Mono has no phone counterpart."* At `dd5551ec`:

```
src/constants/Typography.ts:12    mono: 'monospace'
src/constants/Typography.ts:13    scannerMono: 'ShareTechMono-Regular'
```

`Typography.fontFamily.mono` is spelled at **18** sites and `scannerMono` at **11**, and the
split they draw is the demo's, role for role:

| role | phone sites |
|---|---|
| `scannerMono` | `BiometricScannerHUD.tsx:539,633,638,647,655` · `ImportTerminalProgress.tsx:45` · `TerminalLine.tsx:21` · `DashboardCaseCard.tsx:266,305` · `LocationPill.tsx:194` · `ExportHub.tsx:324` · `app/(tabs)/home.tsx:243` |
| `mono` | `CoordinateDisplay.tsx:254` · `CameraMarker.tsx:188` · `LocationDetailCard.tsx:942` · `TimerCard.tsx:208,228,241` · `LevelMeter.tsx:188,205` · `SpectrumVisualizer.tsx:382,450` · `RecorderScreen.tsx:378` · `AudioPreview.tsx:365` · `MediaPreview.tsx:456` · `ImportResultBody.tsx:356,414,442` · `ImportFlowModal.tsx:674` · `BatchResultDetails.tsx:283` · `CaseActionsSheet.tsx:433` · `ErrorBoundary.tsx:150` · **`ocr-time-capture/ConfirmationScreen.tsx:380`** |

**So the demo's two FAMILIES are the phone's two ROLES — this is a PORT, not the divergence D13
calls it.** What actually diverges is one step smaller and the phone states it itself, at
`app/(tabs)/home.tsx:238-242`, verbatim: *"The only mono face this app bundles. `JetBrainsMono`
was never loaded - app.config.js registers ShareTechMono-Regular alone and there is no useFonts
call anywhere - so this label has silently rendered in the platform sans its whole life (D12b)."*
The phone's `mono` role falls through to the platform generic; the demo loads a real face for it.

**Consequence:** D13's ruling is unaffected and is what the guard encodes — but the record should
say the demo is FOLLOWING the phone's structure, not diverging from it, because the next reviewer
will otherwise re-open A94 every phase. Two of D13's named JetBrains roles (case numbers, export
artifact lines) *are* `scannerMono` on the phone, so on those two the demo follows D13
deliberately; that is the only real divergence and it is one line of the ruling, not the whole of it.

### R-2 — A93's census is TWO SITES. The real figure is 120 lines across 39 files.

A93's Demo-state column names *"**Two demo-only sites**, both user-facing:
`chrome/DemoErrorBoundary.tsx` … and `chrome/PdfPreview.tsx`"*, and its correction block concludes
*"the demo's only real violations are in **demo-only** copy the phone never wrote."*

Measured at the branch point over every `.ts`/`.tsx` under `ui/` excluding `__tests__`, comments
stripped: **120 lines**. After the rule's own data-glyph carve-out and the console-diagnostic
exemption, **82 were real offenders**. The row is sized **S** in the matrix and rides an **M**
package; its census was two orders of magnitude short, and that sizing error is the single
biggest thing an orchestrator should carry forward from this package.

### R-3 — the PHONE is not clean either, and six demo strings are phone-verbatim WITH the dash

A93 says *"Three surviving phone em dashes are all agency-cloud, out of scope both sides."* The
same scan over the phone's `src/` at `dd5551ec` returns **82 lines**, and six are rendered copy the
demo carries byte-for-byte:

| demo | phone |
|---|---|
| `PickerStage.tsx` `pasteTextDescription` | `ImportPickerModal.tsx:920` — byte-identical |
| `PasteStage.tsx` hint | `ImportPickerModal.tsx:721` — D5-adapted ("on-device AI" → "The AI"); the clause with the dash is verbatim |
| `NotesScreen.tsx` stale caption | `SectionBlock.tsx:283` — byte-identical |
| `NotesScreen.tsx` restore banner | `NotesSectionEditor.tsx:212` — byte-identical |
| `FormFieldsPane.tsx` footnote | `FormCustomizationSection.tsx:187` — byte-identical through the clause |
| `CloudSyncPane.tsx` description | `CloudSyncSettingsSection.tsx:128` — byte-identical |

So A93's premise is **inverted**, and on those six the copy rule collides head-on with plan §4.1
rule 7 ("lift copy verbatim").

**The ruling I applied is not mine.** `phone-ui-delta-inventory.md:13300` settles it in the
inventory's own words: *"**Em dashes that SURVIVE** (do not 'fix' them in the demo without an owner
call — they are still in the phone strings at `main`)."* It names only the three agency-cloud
strings because that is all its author had found; the instruction is general and the evidence
covers all six. They are FROZEN, each named with its phone `file:line`, and proposed as deferral
D-1 with the owner call as its trigger.

**Also worth recording:** the owner's rule as originally ruled
(`docs/plans/ui-consistency/02-ui-consistency-owner-rulings.md:188`) ends *"A repo-wide sweep of
existing copy is deferred to a future pass."* Plan §4.3's restatement drops that clause. The demo
IS that deferred pass, so the sweep is right — but the phone enforced the rule **per-PR on copy it
was already rewriting** (P1, P2 D6, P3 D4, P8 D-10, P10 `98ffb98d`), which is why 82 survive there.

### R-4 — `ImportResultBody`'s `0.6/0.7` card is ALREADY on the nested tier. Deferral §31 is closed.

The U7.3 row instructs: *"`ImportResultBody`'s `0.6/0.7` card → the nested tier (**deferral §31's
named site**)."* At the branch point `ImportResultBody.tsx:9-14` already reads
`const card = { ...glassCardNested, padding: '12px 14px', … }`, with U1.3's own comment naming
§31. **U1.3 closed it.** `ImportModal`'s `DataFoundCard` (`:185`) is likewise already
`glassCardNested`. Nothing to do; §9 clause 9's "strike §31 through" is already earned.

### R-5 — `PickerStage`'s error banner is ALREADY a `Banner`. U3.3 did it.

The row instructs: *"`PickerStage`'s error banner → `Banner` (**the phone's own verdict is that
this is the ONLY rendered change in a 440-line diffstat** — do that and stop)."* D19's re-cut
assigned `PickerStage.tsx:317-323` to **U3.3**, which landed it: `PickerStage.tsx:323` is
`{error && <Banner severity="error" message={error} testId="import-picker-error" />}` and
`banner.test.tsx`'s `ADOPTED` list carries the path. The row and the D19 ruling contradict each
other; D19 won, correctly. Nothing to do.

### R-6 — `ImportModal`'s "four glass tiers per depth" is a THREE-part claim and two parts are spent

The row instructs *"`ImportModal`'s four glass tiers per depth (`elevated` ×4, the nested warnings
card `nestedCard`)"*. Measured: `PickerStage`'s `actionCard` (`:80-81`) and its large-batch confirm
(`:277`) both already read `GLASS.borderAccent` + `GLASS.gradientPanel`, which since U1.3 ARE the
`elevated` tier, derived. `DataFoundCard` is `nestedCard` (R-4). The only surviving hand-rolled
surface in the file is `FailuresCard` (`:195`) — and it is a LIST, not a status line, which is
exactly why `banner.test.tsx:246-248` records `ImportModal.tsx` as deliberately absent from BOTH
its `ADOPTED` and `HANDED_BACK` lists. Left alone, per that recorded decision.

### R-7 — `infoRow`'s flat wash does not exist in the demo, so "do not ship one" is satisfied by not building one

The row's clause *"`infoRow`'s flat wash measured 1.03 — do not ship one"* describes phone
`ConfirmationScreen.tsx:322-326`, where a `colors.backgroundTertiary` wash became a third
`<Card glass glassVariant="nestedCard">`. The demo never built that block: its "Actual (atomic)"
value is a bare line **inside** the evidence card (`:398-400`), on no wash at all. Recorded rather
than manufactured.

### R-8 — D17's two ruled value changes: U7.2's refutation re-verified, and it stands

The brief asks me to confirm nothing remains. Re-measured at my branch point:

```
$ grep -rniE '007aff' features/demo/          -> 0 hits   (control: '2b8cc1' -> 30 files)
```

and the phone component behind item (ii), `ocr-time-capture/components/CameraControls.tsx:68-72`,
paints 52pt control circles at `colors.overlay` **inside a full-bleed camera preview**. The demo's
OCR aim stage has no counterpart: Cancel and switch-camera sit BELOW a bounded `aspectRatio`
viewfinder with `background: 'transparent'` and no fill of any kind (`OcrCaptureScreen.tsx:642-700`).
**Both no-ops, confirmed.** I changed neither, and `camera-chrome.test.ts`'s scoped guard still
passes.

### R-9 — the retired `#1e3a5f` was live in the OCR screen, spelled as an `rgba()`

`OcrCaptureScreen.tsx`'s evidence card and its viewfinder frame both carried
`border: '1px solid rgba(30,58,95,0.6)'` — the phone's RETIRED pre-P0 `border` in decimal form.
`tokens/__tests__/palette.test.ts`'s retired-hex sweep is a HEX scan, so it never saw either.
The card's is gone with the tier adoption. **The viewfinder frame's is still there** and is
deliberate — see deferral D-3.

---

## 2. Commits

| Hash | One-liner |
|---|---|
| `ada32cb` | `feat(u7.3): the D13 mono policy, codified and pinned; NotesScreen's forced-dark tokens` |
| `2e98f4d` | `feat(u7.3): the OCR confirm stage — nested tier, app ground, two Banner adoptions` |
| `5ca829a` | `feat(u7.3): the import terminal's three #117 deltas, the scope chips, and D12's defence` |
| `aab81ca` | `feat(u7.3): A93 — the em-dash sweep, and the repo-wide guard that holds it` |
| `ff01669` | `test(u7.3): pin the scope-chip neutralisation (probe P13 SURVIVED)` |
| `18f3e24` | `style(u7.3): name the retired green instead of spelling it (U7.1's D-4)` |

**Two new modules, both `SEAM(U7.3)`:**

- **`features/demo/ui/controls/sample-badge.ts`** — `SAMPLE_BADGE` (`foreground`/`background`/
  `border`). D12's freeze-and-defend arm. Two consumers, zero rendered bytes moved.
- **`features/demo/ui/__tests__/copy-rules.test.ts`** — A93's repo-wide guard. Not a module, but
  it is the deliverable the row names and the place the ruling is written down.

Plus the mono policy as a new `describe` in `ui/__tests__/fonts.test.ts` — the row asks for "a
docblock", and this is where a reader of the *rule* will find it, next to the scanner that
enforces it. There is no token module to put it in because the policy is about which SURFACE may
spell a face, not about the value of the stack (which `fonts.test.ts` already owned).

---

## 3. What shipped, by row

### A94 / D13 — the mono policy

Three lists, in `fonts.test.ts`. `SCANNER_ONLY` (4 files: `BootSequence`, `SplashScreen`,
`ImportTerminalProgress`, `TerminalLine`) must spell Share Tech Mono **and must not** spell
JetBrains. `MIXED` (2 files: `StoryRail`, `OcrCaptureScreen`) legitimately carry both, each naming
the site that earns the scanner face. Every other file under `ui/` must not spell Share Tech Mono.

A one-sided allow-list would have been green over JetBrains creeping INTO the terminal, which is
the drift direction A94 actually found. A `toEqual` over the sorted scan is the dead-exemption test
as well as the violation test — there is no count to edit.

**One violation, found and fixed:** `ImportModal.tsx`'s `TechnicalDetails` `<pre>` painted a raw
failure payload in the SCANNER face. Its exact phone counterpart is `ImportFlowModal.tsx:674` —
`detailsText: { fontFamily: Typography.fontFamily.mono }`, the evidentiary role.

**Three RENDER pins** anchor the scan behaviourally at the one file A94 names for holding both
faces: the AIM caption is `stmono`; the parsed DVR time, the atomic actual and an unreadable
frame's raw text are `jbmono`. A source scan generalises; these observe.

**`NotesScreen` keeps JetBrains**, by D13's own ruling, and it is recorded as demo-ORIGINATED: the
phone's notes components carry **no `fontFamily` at all** (zero hits in
`src/features/documentation/notes/components/` at `dd5551ec`), so its editor renders in the
platform sans.

### B.6 row 37 — the OCR confirm stage

1. **The evidence card → `glassCardNested`** (A33/A34/A35/A55). Was `background:'#0a1320'` behind
   the retired border in `rgba()` form (R-9). The failure view's card takes the same tier — the
   phone's own shape at `ConfirmationScreen.tsx:301`.
2. **The confirm stage moves to the APP ground.** `OcrCaptureFlow.tsx:109-110` is explicit that the
   phone's `ForceColorScheme` wrap *"deliberately does NOT cover the confirmation step above: that
   is a normal themed form screen with no camera behind it."* The demo's confirm stage inherited
   the camera's `#05080d` only because both stages live in one component. **This is not cosmetic:**
   the row's own clause is "do not ship a flat wash", and a translucent tier composited over a
   near-black parent is exactly as unmeasurable as the wash it replaces. The AIM stage keeps
   `#05080d` — that half IS the phone's forced-dark camera step — and a pin says so.
3. **Two Banner adoptions**, D19's `OcrCaptureScreen` hand-back, closed. The assumed-date blocker
   was the family Banner exists to absorb (a translucent `rgba(255,71,87,0.06)` fill under a 15/600
   heading in the saturated accent family); the phone deleted the identical shape one file over and
   gave the reason at `DateDisambiguationWarning.tsx:19-28`. The unreadable-frame view becomes
   Banner + the raw text in a nested card, which is the phone's two-part shape at `:301-304`.
   The confirm control is a **sibling**, per Banner's own docblock and U7.2's Dismiss precedent.
   `banner.test.tsx`'s ledger row moved from `HANDED_BACK` into `ADOPTED`; **four hand-backs
   remain** (U6.2, U6.4a, U6.4b ×2).
4. The assumed-date confirm button joins U2.2's one button recipe (it spelled `#2B8CC1`/`#4BA3D4`
   raw). `disabled` is deliberately NOT passed — D10 keeps the opacity + `aria-disabled` idiom.

### Rows 74 / 79 — the import terminal and the result body (U7.1's deferral A, closed)

| | before | after | phone |
|---|---|---|---|
| jump pill fill | `colors.primary` | `colors.primaryDark` | `:113` |
| jump pill glyph + label | `'#fff'` ×2 | `colors.onPrimary` | `:115-116` |
| processing badge fill | `rgba(26,45,68,0.55)` | `withAlpha(colors.backgroundTertiary, 0.55)` | `:446` |
| processing badge border | `rgba(43,140,193,0.32)` | `withAlpha(colors.primary, 0.32)` | `:445` |
| CTA success title | `#7fe6b6` (row 74's "sixth green") | `colors.successOnLight` | `:242` |
| CTA partial title | `colors.warning` | `colors.warningOnLight` | `:283` |
| CTA failure title | `colors.error` | `colors.errorOnLight` | `:298` |
| CTA borders/washes ×6 | literals | `withAlpha(<token>, a)` — byte-identical | `:243-244,284-285,299-300` |
| scope chip label | `#7fe3b4` / the sample amber | `colors.text` | `:151` |
| scope chip fill | 12% tone | `withAlpha(tone, 0.16)` | `:153` |
| scope chip border | none | `withAlpha(tone, 0.4)`, per-side | `:154` |

The jump pill's reason is the phone's own: *"`onPrimary` on `primary` measures 3.73:1 in dark, and
this pill's label is 13px normal weight, so AA's 4.5 applies with no large-text relief. On
`primaryDark`: 5.80 dark / 8.72 light"* — DEF-UI-001, which D5 inherits only where the phone has
NOT fixed it. Here it has.

**Copy, and it is the PHONE'S OWN em-dash fix:** `Batch complete: …` (`:275`),
`Batch partially failed: …` (`:288`), `Review the import; some files failed` (`:292` — the phone's
PR #117 review lane records the semicolon verbatim as "fixed from an em-dash … per the standing
no-em-dash rule"). The demo-only `sample import — review →` has no phone counterpart and takes the
demo's existing middot idiom.

**New coverage:** the jump pill and the processing badge had **no colour pin of any kind** before
this package — the same gap U7.1's R3 found across `TERM_CHROME`. Both are now pinned by
DERIVATION, never by a re-typed literal.

### D12 — freeze and DEFEND

The "Sample data" amber lived as byte-identical literals in TWO files
(`ImportResultAccordion`, `OcrCaptureScreen`), so a future "re-derive it" ruling would have had to
find both, and a defence test would have had to re-type the values it guards — U7.1's probe-P5
trap. `SAMPLE_BADGE` owns it now; zero rendered bytes moved.

Four measured pins in `palette-contrast.test.ts`, using the file's existing CIE76 `deltaE` and
`flatten`: the badge's **composited** fill is dE > 10 from `warningLight` over the same nested
card; its foreground is dE > 10 from all four ported warning foregrounds; it clears AA on the card
it renders on; and it is structurally NOT any warning token, so a "tidy" reds before the dE rows
silently collapse to 0. **D12 predicts no collision — this measures it.**

### A93 — the sweep

**82 strings rewritten** across 37 files, with a period, comma, colon or parentheses (the owner's
own list). Rewritten for sense, never mechanically: title+body flattens take a **period**, which is
the phone's own precedent at `SecuritySettingsSection.tsx:117-120`; terminal log lines take a colon
or a comma. **Six FROZEN** (R-3). **Two exemptions**: data-glyph placeholders (the rule's carve-out)
and developer console diagnostics — the latter is not a judgement call, since the rule's scope is
stated as "user-facing" four times, the phone left its own `[MediaUpload]`/`[SupabaseConfig]`
console strings em-dashed at `main`, and a PR-122 review adjudication dropped an added em dash
precisely because it was not rendered.

The guard strips comments, then blanks every `console.<method>( … )` call **by matching parens**
rather than by a line heuristic: four of these calls are multi-line concatenations (`motion.ts`,
`phone-overlay.tsx`, `BootSequence.tsx` ×2) and a line-scoped rule would let a rendered string on a
continuation line through. A unit test pins that the matcher stops at its closing paren.

**Three strings from `engine/`, deliberately:** `settings-values.ts:217-219` carried
`AES-256 — Recommended (strong)` and its two siblings. The phone **already fixed exactly these** —
the inventory's copy-changes table at `:13288` gives the before/after verbatim — so they are a copy
PORT under §4.1 rule 7, not a judgement call. The rest of `engine/` is deferral D-2.

### U7.1's deferral B — `NotesScreen`'s forced-dark tokens (closed)

All five constants now read `palette[TERMINAL_SCHEME]` (the U7.1 forced-scheme pattern; the phone
wraps this content in `<ForceColorScheme scheme="dark">` at `NotesSectionEditor.tsx:110` and reads
plain `colors.*` inside it).

| was | now | phone |
|---|---|---|
| `TEXT '#dfe9f3'` | `text` | `SectionBlock.tsx:266` |
| `DIM '#7a93ad'` | **SPLIT** — `textTertiary` / `textSecondary` | `:212` vs `:213` |
| `PRIMARY '#4BA3D4'` | `primaryLight` (exact, named) | `:224,300` |
| `WARNING '#ffd93d'` | `warning` (exact, named) | `:281` |
| `'#c7d6e4'` | `textSecondary` | `:287` |
| `'#a9c2d8'` | `textSecondary` | `:190` |
| `rgba(122,147,173,0.4)` | `border` | `:174` |

`DIM` was ONE demo constant covering TWO phone tokens, so it is **deleted** rather than renamed.
The last row is the retired `DIM` again, spelled as an `rgba()` — a hex sweep missed it, the same
class as R-9.

---

## 4. Mutation probes

Worktree `probe-u7.3-ocr` (`git worktree add … -b probe/u7.3-ocr <head>`, `pnpm install
--prefer-offline` 2.6s). All probes ran with the harness defaults — **motion ON**
(`vitest.setup.ts:47-60`'s `matchMedia` stub is hard-`false`) and `navigator.mediaDevices` left
undefined, i.e. the sample path, which is the tested contract. Nothing here is motion- or
capability-gated. Every verdict is the runner's exit code; every restore is a **file-specific**
`git checkout -- <path>` followed by an asserted-empty `git status --porcelain`.

**18 runs · 17 distinct mutations · 16 KILLED · 1 SURVIVED (fixed, then killed).**

| # | Mutation | Verdict |
|---|---|---|
| P1 | a NEW em dash in rendered copy, in a file the guard never names | KILLED |
| P2 | DELETE a FROZEN string (the dead-exemption half) | KILLED |
| P3 | an em dash on a line FOLLOWING a `console.warn(` — the paren matcher must stop | KILLED |
| P4 | the scanner face moved onto an evidentiary surface (`ImportResultBody`) | KILLED |
| P5 | JetBrains creeping into a wholly-chrome file (`TerminalLine`) | KILLED |
| P6 | `SAMPLE_BADGE.foreground` tokenised to `warningDark` (D12 violated) | KILLED |
| P7 | the badge fill CONVERGES on `warningLight` without being identical | KILLED |
| P8 | the confirm stage's ground reverts to the camera's near-black | KILLED |
| P9 | the evidence card reverts to the hand-rolled slab | KILLED |
| P10 | the jump pill drifts back to the flat mid-tone | KILLED |
| P11 | the processing badge's orphan fill re-inlined at its OLD value | KILLED |
| P12 | the CTA success title reverts to the retired "sixth green" | KILLED |
| P13 | the scope chip's label goes back to the tone | **SURVIVED** → fixed in `ff01669` |
| P13r | same mutation, after the fix | KILLED |
| P14 | the chip loses its colour coding entirely (fill neutralised too) | KILLED |
| P15 | the assumed-date Banner reverts to a hand-rolled callout | KILLED |
| P16 | the AIM caption takes the evidentiary face | KILLED |
| P17 | `NotesScreen`'s body text back to its private near-copy of `text` | KILLED |

### The one SURVIVED, in full

```
MUTATION PROBE: the scope chip's label is neutralised, not painted with the tone
Target:      features/demo/ui/screens/ImportResultBody.tsx — ScopeRow's scopeTag
Claimed pin: ImportResultBody.test.tsx — "renders scope rows with the time tag and the
             canonical range"
Mutation:    `color: colors.text` -> `color: tone`
Result:      SURVIVED  (from exit code 0)
             scoped (ImportResultBody): 5 passed — exit 0
Path taken:  the file's ONLY chip coverage was `getByText('DVR TIME')` and
             `getByText('ACTUAL TIME')` — presence assertions, green whatever colour the chip
             is painted. Row 79's neutralisation had shipped with NOTHING observing it, so
             re-pointing the label back at the tone was invisible to the entire suite. The
             change-detector trap in reverse: presence asserted, treatment unasserted.
Restore:     verified byte-identical (`git status --porcelain` empty)
```

Fixed in the **TEST**, never the mutation. The replacement pins assert the **split** rather than a
colour: the LABEL is `colors.text` and explicitly NOT the tone, while the fill and all four border
sides still carry it — the phone's actual ruling at `ImportResultBody.tsx:145-159` ("the tint and
border keep the colour coding; the label carries the words"). A pin on the label alone would have
been green over a chip that lost its colour coding entirely, which is why P14 exists.

**Teardown, proof line quoted below in §8.**

---

## 5. Gates (cold, at `18f3e24`)

| Gate | Result |
|---|---|
| `pnpm test --silent` | **exit 0** — 295 files / **3,959 passed** / 4 todo (3,963) |
| `rm -f tsconfig.tsbuildinfo && pnpm exec tsc --noEmit --incremental false` | **exit 0** |
| `node .design-sync/check-rn-parity.mjs` | **exit 0** — *"all 135 anchor rows match"* |
| `palette-contrast.test.ts` + `rn-token-parity.test.ts` | green; the RN guard RESOLVED (it did not skip) |
| `pnpm build` (after `rm -rf .next`) | **exit 0** · **`/demo` First Load JS = 107 kB** — the pinned figure, unmoved |

Branch-point baseline was **3,932** tests. Net **+27**, all additions; **nothing deleted, nothing
weakened**. `census.mjs` run: `#7fe6b6` **0**, `#7fe3b4` **0**, retired `#1e3a5f` **0** — all
outside `__tests__` and outside comments.

**Zero behavioural diffs.** The OCR recognition pipeline is untouched; `STRIP_TOP`/`STRIP_SIDE`
(demo §0.2's frozen list) are byte-unchanged; the R-4 generation token, the R-15/R-23/R-35
`aria-disabled` shapes, the R-16 sample-score disclosure and the landscape-viewfinder directive all
stand. The only prop signature that moved is none — D20 was not invoked.

---

## 6. Visibly changed surfaces

For the reviewer's before/after set.

1. **OCR confirm stage — the ground.** Near-black `#05080d` → the app navy. The single most
   visible change in the package.
2. **OCR confirm stage — the evidence card.** A flat near-black slab with a hairline → the nested
   glass tier (gradient, lit top edge, inset).
3. **OCR assumed-date blocker.** A translucent red box with a 15/600 red heading → an opaque
   `error` Banner with a leading glyph, the confirm button now a sibling `outline` control below it.
4. **OCR unreadable-frame view.** Same transformation, plus the raw text moving into its own
   nested card.
5. **Import terminal — the jump pill.** Mid-blue → the deeper `primaryDark`.
6. **Import terminal — the processing badge.** A 55% dark-navy slab (the orphaned pre-recolor
   tertiary) → 55% of the current tertiary. Lighter and on-ramp.
7. **Import terminal — the outcome CTA titles.** Green / amber / red → near-white. The accent stays
   on the icon. Three surfaces change character; the phone's ruling says they must.
8. **Import result — the scope chips.** Coloured labels → `colors.text` labels, with the tone
   moving into a stronger 16% fill plus a new hairline border.
9. **Notes panel.** Body text, the stale preview, the addendum text and the addendum's left rule
   all move onto palette tones — the addendum rule goes from a light slate to the navy `border`,
   which is the most noticeable of the five.
10. **~82 strings across the whole demo** lose an em dash. Individually invisible; collectively the
    thing the owner's rule exists for.

---

## 7. Deferral PROPOSALS (house format — `dt-review-aggregator` decides and writes)

### D-1 — six phone-verbatim strings keep their em dash, pending an owner call

- **Source:** U7.3 report R-3; `features/demo/ui/__tests__/copy-rules.test.ts`'s
  `FROZEN_PHONE_VERBATIM`.
- **What:** `PickerStage.tsx`'s `pasteTextDescription`, `PasteStage.tsx`'s hint, `NotesScreen.tsx`'s
  stale caption and restore banner, `FormFieldsPane.tsx`'s footnote and `CloudSyncPane.tsx`'s
  description each carry an em dash **because the phone string they are lifted from still does at
  `dd5551ec`** (`ImportPickerModal.tsx:920`/`:721`, `SectionBlock.tsx:283`,
  `NotesSectionEditor.tsx:212`, `FormCustomizationSection.tsx:187`,
  `CloudSyncSettingsSection.tsx:128`).
- **Why deferred:** two ratified rules collide on exactly these six — plan §4.3's copy rule and
  §4.1 rule 7's "lift copy verbatim" — and `phone-ui-delta-inventory.md:13300` rules the tie
  explicitly: *"do not 'fix' them in the demo without an owner call — they are still in the phone
  strings at `main`."* Sweeping them would manufacture NEW demo↔phone copy drift inside a parity
  port, on a surface the owner will see side by side at the W4 checkpoint.
- **Trigger:** **the owner call the inventory names**, at the next device checkpoint (D1) — or
  the phone fixing any of the six upstream, whichever fires first. Each is one line, and the
  guard's `FROZEN_PHONE_VERBATIM` list is where the answer is recorded either way. The
  dead-exemption test guarantees the list cannot silently rot.

### D-2 — `engine/`'s user-facing strings are outside A93's swept scope

- **Source:** U7.3 report §3, A93; measured over `features/demo/engine/` at `18f3e24`.
- **What:** **82 lines** carry an em dash, of which roughly 40 are rendered user-facing copy —
  `logic/media/permissions.ts:106-124` (six capture-failure messages), `logic/ocr.ts:276-279` (the
  four confidence messages), `logic/gps.ts:95`, `logic/media/captured.ts:174`,
  `logic/datetime-normalize.ts:114-115`, `content/narration.ts:376`, `content/profiles.ts:101,103`,
  `content/seed.ts` (the sample request body) and two `logic/pdf/case-notes.ts` document warnings.
  The three `settings-values.ts` AES labels were swept here because the phone had already fixed
  those exact strings; the rest have no such citation.
- **Why deferred:** the U7.3 row scopes A93 to *"every user-facing string in `ui/**`"* and that is
  what the guard's scan root is. `engine/` sits under the 80% coverage gate with heavy assertion
  coverage, several of these strings are asserted by engine unit tests, and `content/seed.ts` is
  fixture prose whose em dashes are *in-character* for a detective's email — a judgement the row
  does not authorise an implementer to make alone.
- **Trigger:** **U8's closing fidelity pass**, which already owns a census sweep, OR the first
  review round that flags an engine string. Widening the guard is a ONE-LINE change (`UI_ROOT` →
  both roots) and the guard will then name every offender with its line, so the work is the copy
  decisions, not the mechanism.

### D-3 — the OCR viewfinder frame still spells the retired border as an `rgba()`

- **Source:** U7.3 report R-9; `features/demo/ui/screens/OcrCaptureScreen.tsx` (the landscape
  viewfinder's `border`).
- **What:** the viewfinder box carries `border: '1px solid rgba(30,58,95,0.6)'` — the phone's
  RETIRED pre-P0 `border` in decimal form, which `palette.test.ts`'s HEX sweep cannot see. The
  evidence card's copy of the same value is gone with the tier adoption; this one is not.
- **Why deferred:** it is on the **AIM stage**, i.e. inside the phone's forced-dark camera step,
  which **D17 freezes** and where `camera-chrome.ts` — not the palette — is the owner. Re-pointing
  it at `colors.border` would theme a camera surface (A91's explicit rider: "do not tokenise them
  to the app ground"), and adding it to `CAMERA_CHROME` is a third D17 value change an implementer
  should not rule on, exactly as U7.2 declined for `guideMask`. **The real finding underneath is
  the guard's**, and it is general: the retired-hex sweep is hex-only, so every retired colour
  spelled as `rgba()` is invisible to it repo-wide.
- **Trigger:** whichever fires first — the owner's next D17 ruling (which would also settle U7.2's
  `guideMask` deferral, its sibling), OR **any package that widens `palette.test.ts`'s RETIRED
  sweep to decimal `rgba()` forms**, which is the change that turns this from a note into a red.
  Both this site and `guideMask` land in the same commit when it does.

### D-4 — `ImportModal`'s `FailuresCard` is the last hand-rolled severity surface in the import flow

- **Source:** U7.3 report R-6; `features/demo/ui/screens/ImportModal.tsx` (`FailuresCard`).
- **What:** it is a `GLASS.borderError` + `rgba(255,71,87,0.08)` box — the translucent-fill family
  `Banner` exists to absorb — but it renders a LIST (one row per failed file), not a status line.
- **Why deferred:** `banner.test.tsx:246-248` records `ImportModal.tsx` as deliberately absent from
  BOTH `ADOPTED` and `HANDED_BACK`, with U3.3's reasoning: a Banner's `message` is one paragraph of
  plain text and *"anything needing a control beside it puts one there"* — a list is the same
  problem. Converting it needs either a Banner + a sibling list (changing the visual grouping the
  failures currently have) or a `children` prop on `Banner`, which the phone has never had and
  which U3.3's docblock argues against at length.
- **Trigger:** the SECOND surface that needs a severity callout wrapping structured content. At
  that point the pair justifies a decision — either a shared "severity card" recipe distinct from
  `Banner`, or the explicit ruling that these stay hand-rolled. One consumer is not enough to
  design against.

---

## 8. Teardown

`probe-u7.3-ocr` removed via `tools/worktree-remove.ps1`, proof line quoted:

```
node_modules/.pnpm entries BEFORE: 240
unlinked 549 junction(s) in 2 pass(es)
node_modules/.pnpm entries AFTER : 240
OK -- worktree removed, main checkout's .pnpm store intact (240 entries).
```

exit 0. Branch `probe/u7.3-ocr` deleted; `git worktree list` carries no `probe-*` entry.

---

## 9. CONSUME-ME for the next agent in this territory

1. **The mono policy is a LEDGER, not a count.** `ui/__tests__/fonts.test.ts`'s `SCANNER_ONLY` /
   `MIXED`. If you add a Share Tech Mono site, add the file with its reason; if you delete one, its
   row must go too. A `toEqual` over the sorted scan fires on both directions.
2. **A93's guard will red you on any new em dash in `ui/**`**, including one you write in a
   *rendered* string on a continuation line after a `console.warn(`. Its failure message tells you
   the three allowed replacements and where the FROZEN list is. `engine/` is NOT scanned (D-2).
3. **Comments are source to FOUR scans now**, and this package tripped three of them with its own
   prose: the banned-literal sweep, `palette.test.ts`'s retired-hex sweep, and the mono policy. If
   you explain a hex, name the token instead of writing the hex. U7.1's D-4 is the general rule and
   it has now cost two packages a red each.
4. **`SAMPLE_BADGE` is D12's defended amber.** `ui/controls/sample-badge.ts`. Two consumers. Do not
   tokenise it to the warning family — four pins in `palette-contrast.test.ts` measure the
   separation, and one of them exists purely so a tokenisation reds *before* the dE rows collapse
   to zero and start passing vacuously.
5. **`banner.test.tsx`'s ledger has FOUR hand-backs left:** `TimeOffsetScreen` and
   `CompletionScreen` (U6.4b), `NewCaseModal` (U6.4a), `_pane-chrome` (U6.2). When you adopt,
   delete your `HANDED_BACK` row and add the path to `ADOPTED` — the test fires on the adoption.
6. **The A93 sweep touched files U5 and U6 hold open.** One line each, per plan §6.1's "bounded to
   the offending string per file": `screens/map/{MapScreen,LocationList,CaseMapPicker}.tsx`, nine
   settings panes, `NewCaseModal.tsx`, `ExportModal.tsx`, `MediaCaptureScreen.tsx`. **U5.2 DELETES
   some of those map surfaces outright** — resolve toward U5's deletion, the copy edit is worthless
   on a deleted node. Twenty `__tests__` files also carry updated copy assertions.
7. **This branch is off the PRE-carry `feat/uiparity-u7` head** (`02bba18`) and deliberately merged
   nothing. W2's F26–F50 fixes and the phase branch's later carry are not in it.

---

## 10. Two likely defects (where a reviewer should look hardest)

1. **The confirm stage's ground change is the one judgement call in the package, and its blast
   radius is larger than its diff.** Moving `OcrCaptureScreen`'s confirm shell from `#05080d` to
   `colors.background` is one line, phone-cited (`OcrCaptureFlow.tsx:109-110`) and pinned — but
   every OTHER value on that screen was chosen against a near-black ground: `label12`'s
   `#7a9fc4`, the `#9fc0db` bodies, the `#cfe6f5` and `#cdd9e6` values, the sample-note `#7a9fc4`
   at 11px. Those now composite over the app navy, and **nothing in this repo measured them
   there** — `palette-contrast.test.ts`'s `DARK_GROUNDS` covers tokens and tiers, not a screen's
   loose literals. The tier and the ground are right; the six foregrounds sitting on them are
   inherited and unverified. If the owner's side-by-side flags this screen, that is the list.
2. **The A93 rewrites are 82 independent editorial judgements and only their ABSENCE is tested.**
   The guard proves no em dash survives; nothing proves a replacement reads well or preserves the
   sentence's meaning. Two shapes are worth re-reading specifically: the title+body flattens in
   `DemoExperience.tsx` (`'Location Duplicated. Main Store - Copy created with scopes.'` and its
   eight siblings), where a period between a Toast title and its body can read as a sentence
   fragment on a narrow notification; and the four parenthetical rewrites in the settings panes
   (`ExportSecurityPane`, `SecurityPane`, `MediaCapturePane`, `exportNotices.ts:37,146`), where I
   converted a dash pair to parentheses and the clause boundaries moved. Both are copy, not code,
   so a reviewer reading the diff is the only mechanism there is.

---

## 11. Corrections owed to the plan / matrix (orchestrator, at merge)

1. **A94** — "the phone has exactly ONE mono face" → **two declared roles, both live**
   (`Typography.ts:12-13`; 18 + 11 sites). The demo's split is a PORT of the phone's; the
   divergence is only that the demo loads a real face where the phone falls through to the platform
   generic (phone's own note, `app/(tabs)/home.tsx:238-242`). (R-1)
2. **A93 Demo-state column** — "two demo-only sites" → **120 lines / 39 files**, 82 real offenders.
   Re-size the row; it is not an S. (R-2)
3. **A93 riders** — "three surviving phone em dashes, all agency-cloud" → **82 lines** in the
   phone's `src/`, six of them rendered copy the demo lifts verbatim. The inventory's own
   `:13300` instruction ("do not fix without an owner call") governs those six. (R-3)
4. **Plan §4.3** — the owner's rule as ruled ends *"A repo-wide sweep of existing copy is deferred
   to a future pass"*; §4.3's restatement drops that clause. Restore it, and record that the phone
   enforced the rule per-PR on copy it was already rewriting.
5. **U7.3 row + deferral §31** — "`ImportResultBody`'s `0.6/0.7` card → the nested tier" is
   **already done by U1.3**; §31 is closed and §9 clause 9's strike-through is earned. (R-4)
6. **U7.3 row vs D19** — "`PickerStage`'s error banner → `Banner`" is **U3.3's, already landed**.
   The row and D19 contradict each other. (R-5)
7. **U7.3 row** — "`ImportModal`'s four glass tiers per depth" → **two of three already spent**
   (U1.3); the only survivor is `FailuresCard`, which `banner.test.tsx` records as deliberately
   unconverted. (R-6)
8. **U7.3 row** — "`infoRow`'s flat wash … do not ship one" → the demo **never built that block**;
   its recorded-time value is a bare line inside the evidence card. Satisfied by construction. (R-7)
9. **B.6 row 74 / U7.1's deferral A** — closed here, including the CTA copy, which is the PHONE's
   own em-dash fix (`:275`, `:288`, `:292`), not an invention.
10. **B.6 row 45 / U7.1's deferral B** — closed here; `DIM` covered TWO phone tokens and is deleted
    rather than renamed, and two further un-tokened one-offs plus an `rgba()` copy of `DIM` were
    found in the same file.
11. **`tokens/__tests__/palette.test.ts`** — the retired-hex sweep is HEX-ONLY, so every retired
    colour spelled as a decimal `rgba()` is invisible to it. Two live instances were found in one
    file (R-9); one is fixed, one is D-3.
