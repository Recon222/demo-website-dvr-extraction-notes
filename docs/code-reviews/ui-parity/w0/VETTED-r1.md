# Vetted Review — W0 (phase U0, PR #39 `feat/uiparity-u0` @ `7099e54` vs `master`) — Round 1

**Verdict:** REVISE
**Lanes read:** typescript 0/1/2/1 REVISE · web 0/1/2/2 REVISE · tests 0/2/2/2 REVISE · silent-failures 0/0/3/1 APPROVE-wc · type-design 0/0/2/2 APPROVE-wc · verification seat (`_captures/w0/DIFF.md`, no labels — 104 changed / 0 required-missing / 0 U0 regressions, four sequencing notes, one gap)
**After dedupe:** 0 critical · 3 high · 4 medium · 3 low  (25 raw lane items → 10 findings)
**Unsettled (operator escalation):** 0

Aggregator: fresh seat for this build (first round of the v2 campaign — no prior VETTED to inherit; F-IDs start at F1). Wrote the raw-findings table to disk first, then this document.

## Unsettled — for the operator (never affects the verdict)

None. The one genuine severity dispute (typescript MEDIUM vs tests HIGH on the unanchored palette keys) was settled by an executed probe — see F2 and Pipeline notes. Two authors' rulings requested in the PR body were answered identically by two lanes each and I concur (recorded under Dropped / demoted).

## Findings

### F1 [HIGH] `GLASS.accentFrom` was re-based to a FILL shade but six shipped sites spend it as a FOREGROUND mark — every one now fails its WCAG floor, and the active media tab is dimmer than its inactive siblings
Lanes: web — original label: HIGH (+ web LOW "hoist withAlpha", folded in as a touch-point)
File: `features/demo/ui/glass-tokens.ts:34` (the re-base `#35A0D6 → #1F6B99`) — consumers:
  `features/demo/ui/screens/MediaLibrarySheet.tsx:221` (active-tab 2px underline), `:222` (active tab label 13px/700), `:240` (tab badge numeral 10px/700), `:571` (selected-row 2px borderLeft);
  `features/demo/ui/screens/ExportModal.tsx:157` (spinner arc); `features/demo/ui/screens/export/ExportCaseCard.tsx:127` (expanded-card outline) and `:132` (the derived glow).
Issue: U0.3's re-base is correct for the CTA fill it was measured against (white ON the gradient 2.94 → 5.80), but the token is also spent as text/rule colour, where darkening it against the (now lighter) ground runs the other way. Aggregator recompute, independent implementation: active tab label on `colors.background` **5.92 → 2.54** (AA text 4.5); badge numeral on its `rgba(43,140,193,0.16)` wash **4.83 → 2.06**; spinner arc vs its own track **4.21 → 1.81** (non-text 3.0 — and under reduced motion the ring does not rotate, so track separation is the whole affordance); inactive `#7a9fc4` labels measure 5.31, so the SELECTED state is now the least legible thing in the control. Reproduced visually by the lane in `_captures/w0/{before,after}/06-media/12-s4-library-tabs.png`. Every accent-as-text contrast row is `it.todo` pending U1.1, so the gate is green over it. Completeness: `grep accentFrom|ACCENT_FROM` under `features/demo` finds exactly these six foreground sites plus the two fills (`gradientAccent`, `T.accentFrom` — zero readers); there is no seventh.
Fix: keep `GLASS.accentFrom` for fills only. Re-point the six foreground uses at `colors.link` (`#b8d4f0` — 9.60 on `colors.background`, the token `palette.ts:91-94` names for "interactive labels and their 1px outlines") or, if the design wants a saturated mark, `colors.primaryLight` (`#4BA3D4`, 5.24). While in `ExportCaseCard.tsx:130-132`, hoist the `withAlpha(...)` glow to a module const (it is prop-independent; repo convention `components/marketing/phone-frame.tsx:30-31`). No new test row is prescribed: after the fix the token has no foreground consumer to measure.
Owner: `ae5f52b4da850cd08` (U0 implementer A — U0.3 territory)

### F2 [HIGH] The drift guard's key set is a hand-maintained list that nothing checks against the palette: 17 of 32 keys are unanchored (15 owned by no package), and the "pins every palette key" test only pins the list's LENGTH
Lanes: typescript — original labels: HIGH ("anchor SET unpinned — only cardinality") + MEDIUM ("fifteen palette keys carry no anchor and no owning package"); tests — original label: HIGH ("guard covers 15 of 32 palette keys"); tests LOW ("docblock says 35 rows") folded in as a touch-point
File: `.design-sync/check-rn-parity.mjs:238-254` (`PALETTE_KEYS`), `:297` (docblock "35 rows" — the table is 33); `features/demo/ui/inputs/__tests__/rn-token-parity.test.ts:86-101` (the three "pins" — all iterate `PALETTE_KEYS`, so the only non-tautological assertions are `length === 15` / `anchors.length === 33`)
Issue: two independent SURVIVED probes on the same root. (a) tests: re-basing `palette.ts:103` `overlay` to a black scrim — a key the guard does not carry — leaves the standalone guard 33/33 OK and all 9 cases green; the comparison is symmetric, so the phone moving `Colors.dark.borderLight` (which renders today via `GLASS.borderBtn`, `SyncStatusCard.tsx:49`, `_shared.tsx:563`) is equally invisible. (b) typescript: swapping `'link'` for `'card'` in the list keeps every count and every loop green — nothing asserts membership. The plan's staging rule (§6.6 gate 1: never anchor a token whose web side does not exist) does not cover these — U0.1 created all 32 web tokens in this same PR — and matrix A96's rule ("adding an anchor is the closing act of the package that creates its web token") is violated with no future package to pick them up: the plan's ~44-key arithmetic never reaches `borderLight, borderDark, successDark, warningDark, info, infoDark, onPrimary, onError, linkHover, card, modal, overlay, overlayLight, disabled, disabledText`. The port's mechanical gate (A96) is true of 47% of the palette. AGGREGATOR PROBE (in-memory, tree untouched): pushed all 17 missing keys onto the exported `PALETTE_KEYS` and ran the real `checkParity()` — **67 anchors, 0 drift, 0 PARSE-FAILED**; the only scheme-invariant keys (which would trip the light≠dark structural pin at `:120-127`) are `onPrimary` and `onError`. Anchoring is a green no-op on landing; there is no cost argument for waiting.
Fix: make `PALETTE_KEYS` every key in `palette.dark` (all 32 today — either derive it from the palette or list it and assert `[...PALETTE_KEYS].sort()` equals `Object.keys(palette.dark).sort()` in the test, which pins MEMBERSHIP and retires both cardinality pins as derived numbers). Give the light≠dark structural pin its own by-name exclusion set `{onPrimary, onError}` — the remedy the test's own comment at `:117-119` already prescribes. Keep `expect(anchors.length).toBe(<new total>)` (it still covers deletion of the three non-palette rows). Fix the `:297` "35 rows" docblock and the `:216-237` schedule in the same commit. `success`/`warning` may join now or stay with U3.1 per the schedule — either is green; the docblock must say which (U0.4 report P-3 already flags U3.1's row as stale — orchestrator propagates). PRESCRIPTION-UNVERIFIED (the membership assertion is a prescribed test no lane executed; the underlying anchoring was probed by me).
Owner: `adff9eb9a7670742f` (U0.4 — guard)

### F3 [HIGH] The two source-text literal scans normalise insufficiently: the `BANNED` scan is whitespace-sensitive (SURVIVED on shipped code) and the `RETIRED` sweep is case-sensitive on its needle (SURVIVED on guard data)
Lanes: tests — original label: HIGH ("banned-literal scan is whitespace-sensitive"); silent-failures — original label: MEDIUM ("RETIRED sweep matches its needle CASE-SENSITIVELY")
File: `features/demo/ui/__tests__/glass-tokens.test.ts:132-134` (`text.toLowerCase().includes(literal.toLowerCase())` — no whitespace normalisation); `features/demo/ui/tokens/__tests__/palette.test.ts:141-146` (`text.toLowerCase().includes(hex)` — needle raw, docblock at `:35-39` says "matched case-insensitively")
Issue: (a) tests probe: re-inlining `'1px solid rgba(28, 78, 132, 0.5)'` — the phone's own rgba spelling, the one `check-rn-parity.mjs:63`'s `norm` exists to absorb — at `controls/AlertDialog.tsx:148` passes the guard (11/11 green), while the identical unspaced uppercase re-inline is KILLED. `map/mapTokens.ts` already writes ~20 spaced rgba values outside `TOKEN_MODULES`, and U1.1's 24 tier keys are all spaced rgba transcribed from `Colors.ts`. (b) silent-failures probe: an uppercase `RETIRED` needle (`'#35A0D6'`, the spelling `BANNED` and `glass-tokens.ts` use for the same values) matches nothing and the sweep is green with the retired hex live in `CasesScreen.tsx`; today's seven needles work only by author discipline, and the U0.5 successor note instructs every later package to append to this list. Same root cause, two axes: the scans compare raw strings where the sibling guard compares normalised ones.
Fix: normalise BOTH sides of BOTH loops — lowercase + strip whitespace (`.replace(/\s+/g, '')`) on needle and haystack, the same treatment as `norm`. Do NOT re-space the demo's literals instead (several are pinned byte-exactly at `glass-tokens.test.ts:145-181`). Re-run the tests lane's probe (spaced re-inline at `AlertDialog.tsx:148`) to confirm the kill before pushing.
Owner: `aaa5c5ea7ea00825b` (U0.5 — literal guards)

### F4 [MEDIUM] `region()` slices more than it claims: `readField` matches inside `//` comments (SURVIVED), and a missed `before` marker silently widens the slice to EOF while the docblock says it throws
Lanes: tests — original label: MEDIUM ("region()/readField read commented-out values"); silent-failures — original label: LOW ("`before` marker silently falls back; docblock says it throws"); typescript out-of-lane observation agrees on the second
File: `.design-sync/check-rn-parity.mjs:113-126` (`region`, esp. `:121-124`), `:113-172` (`readField`), docblock claim at `:100-102`
Issue: (a) `readField` regex-matches the first `key: '<value>'` in the sliced text and nothing strips comments, so a refactor that leaves `// was text: '#f0f4f8'` above a changed value makes the guard read the COMMENT — probe: `text.dark` re-based with the old value commented above it → drift 0, 9/9 green (the inverse, a `// TODO: text: '#ffffff'`, is a false red). The web side is backstopped by `palette.test.ts`'s shape pins; the RN side has no pin and `Colors.ts` is comment-dense, which is where U1.1's tier reads land. (b) `after` throws on a miss (→ PARSE-FAILED, the U0.0 degrade); `before` does not, so a key absent from its intended block and present later in the file is read from the wrong literal instead of reported. Equivalent today (every key first-matches in the intended block); becomes reachable the moment a `before` bound must exclude a later block that also defines the key — `rnTierScope`'s `before: '}'` at U1.1.
Fix: in `region()`, strip line comments before matching (`out = out.replace(/\/\/[^\n]*/g, '')` — every real field sits on its own line, so all 33 rows resolve identically) and throw `region end marker not found: <before>` on a missed `before`, matching the `after` arm. Two lines; makes the docblock true.
Owner: `adff9eb9a7670742f` (U0.4 — guard)

### F5 [MEDIUM] The `T`-alias test compares two strings, so it cannot tell an alias from a re-typed literal for the 5 keys whose hex is not `BANNED` (SURVIVED)
Lanes: tests — original label: MEDIUM
File: `features/demo/ui/tokens/__tests__/palette.test.ts:151-166` (`expect(T[tKey]).toBe(colors[paletteKey])`)
Issue: de-aliasing `input-theme.ts:29` `textMute: colors.textSecondary → '#99badd'` passes all 20 cases across the three token suites; the control de-alias on `bg` is killed — by the `BANNED` scan, not by this test. `text`, `textMute`, `textFaint`, `primary`, `error` are exactly the unchanged high-frequency hexes U0.5 deliberately left unbanned (ledger §93), so nothing catches a de-alias on them, and `input-theme.ts` is U2.1's to rewrite next wave. The test's title claims sourcing; it proves equality.
Fix: make the pin structural, the sanctioned idiom here (`chrome-scope.test.tsx`, `backdrop.test.ts`): read `input-theme.ts` and assert each alias's right-hand side matches `\b<tKey>:\s*colors\.<paletteKey>\b`, one line inside the existing loop beside the value check. PRESCRIPTION-UNVERIFIED (the lane probed the defect, not this pin — verify it kills the `textMute` de-alias before adopting).
Owner: `aaa5c5ea7ea00825b` (U0.5 — `palette.test.ts`)

### F6 [MEDIUM] The colour helpers degrade silently on inputs they half-understand: `flattenOver` returns an uncomposited colour on zero grounds / a translucent last ground (SURVIVED), returns `top` on an unparseable layer with no breadcrumb, and `parseColor` passes `#rrggbbaa` through with its OWN alpha
Lanes: type-design — original label: MEDIUM ("`flattenOver`'s last-ground-opaque invariant is prose-only; zero-grounds arm"); silent-failures — original label: MEDIUM ("`flattenOver` / `withAlpha` return a plausible wrong value, no dev-warn"); typescript — original label: LOW ("`parseColor` accepts 3/6-digit hex only; `#rrggbbaa` returned unchanged"); web out-of-lane note (rgb() regex has no trailing `$`)
File: `features/demo/ui/tokens/scale.ts:151-165` (`flattenOver`, arms at `:152` and `:156`), `:121-126` (`withAlpha`, `:123`), `:85-88` (`parseColor` — rgb regex unanchored, hex alternation `{3}|{6}` only)
Issue: type-design probe: `contrast('#ffffff', ['rgba(0, 0, 0, 0.1)'])` measures 21.00 — identical to pure black — because the single ground's alpha is discarded per the docblock's "last ground treated as opaque"; a U1.1 ground stack that bottoms out on a glass stop instead of `background` is one token away and the file's own `:191` note forbids exactly that, yet nothing enforces it and `stack.forEach(parse)` cannot (the layer parses fine). Silent-failures: an unparseable layer (`transparent`, an 8-digit hex, any `color-mix()`) returns `top` still translucent, and `withAlpha('#2B8CC125', 0.5)` returns the input at alpha 0.15 — the same "requested alpha silently ignored" class the docblock says the port fixed for `rgba()` inputs; the demo already renders four `#rrggbbaa` values (`map/LocationDetailCard.tsx:43`, `map/LocationRow.tsx:22,23,26`) that U5.4 routes through `withAlpha`. Latent today (the one production caller passes a valid hex), which is why MEDIUM; the use-day is U1.1/U5.4 and that day has no reviewer for these lines.
Fix: (1) `flattenOver(top: string, ground: string, ...rest: string[])` — the empty case becomes a compile error, no new machinery; (2) in the contrast test's `flatten()` (`palette-contrast.test.ts:98-102`), assert the last layer's parsed alpha is 1 next to the existing `stack.forEach(parse)` — PRESCRIPTION-UNVERIFIED (type-design probed the defect; confirm the 10%-black case now throws); (3) a dev-only `console.warn` on the two silent arms (`:156`, `:123`), the `generateExtractedScopes` shape — no signature or return-contract change; (4) anchor the rgb regex with `$` and either accept `{8}` hex into the alpha slot or add the known-behaviour case to `scale.test.ts:58-61` so the next reader sees it is known.
Owner: `ae5f52b4da850cd08` (U0 implementer A — U0.2 territory)

### F7 [MEDIUM] `ACCENT_FROM` re-types `colors.primaryDark` (`#1F6B99`) with no structural link; the only thing relating them is a guard that reports green when the phone repo is absent
Lanes: type-design — original label: MEDIUM
File: `features/demo/ui/glass-tokens.ts:34` (and `features/demo/ui/tokens/palette.ts:47`)
Issue: the phone models the top stop as a REFERENCE (`[Colors.dark.primaryDark, '#17527A']`); the demo now holds the hex twice with nothing comparing them — `palette.test.ts:59` and `glass-tokens.test.ts` each pin their own literal. On a run without the sibling repo (ledger §91), re-basing `primaryDark` in `palette.ts` leaves the CTA's top stop, `gradientAccent`, `glassBtnPrimary` and `ExportCaseCard`'s glow on the retired colour with a fully green suite — and the live contrast rows at `palette-contrast.test.ts:307-317` measure against `GLASS.accentFrom`, so the AA claim moves WITH the stale value. The lane PROBED the fix three ways: `satisfies` added → `tsc` exit 0; `palette.ts:47` mutated to `#1F6B9A` → KILLED (TS1360); guard with the fix in place → 33/33, `readConst` ignores the trailing `satisfies`. The comment at `:31-32` correctly rules out `= colors.primaryDark` (the guard reads literals) but does not rule out the type-level link.
Fix: `const ACCENT_FROM = '#1F6B99' satisfies typeof colors.primaryDark`. One line; `ACCENT_TO` has no palette sibling and stays plain. Extend the `:31-32` comment by one clause saying why the link is `satisfies` and not `=`.
Owner: `ae5f52b4da850cd08` (U0 implementer A — U0.3 territory)

### F8 [LOW] `T.borderSoft` and `T.radius` are DEAD keys (0 readers) hard-coding a dark value with a light sibling, and the comment justifying the hand-typed `GLASS.borderSoft` is false as of this PR
Lanes: type-design — original label: LOW ("`T.borderSoft` / `T.radius` are dead keys"); typescript — original label: MEDIUM ("`borderSoft` hand-derives `colors.border` at 50% as a literal, in two copies"); web — original label: MEDIUM ("two hand-typed rgba() copies of `colors.border`"); silent-failures — original label: MEDIUM ("alpha-derived tints are hand-transcribed rgb copies of palette tokens"). Four lanes; DEMOTED — see Dropped / demoted.
File: `features/demo/ui/inputs/input-theme.ts:25` (`borderSoft`), `:43` (`radius: 12`); `features/demo/ui/glass-tokens.ts:51-52` (the comment + `GLASS.borderSoft`); same class at `glass-tokens.ts:54-55`, `input-theme.ts:35,38` (primary/error/link at an alpha — values unchanged, D3)
Issue: `grep -rn 'T\.borderSoft|T\.radius\b'` across `features/ app/ lib/` returns nothing — both keys were re-based instead of deleted, and `borderSoft` is the D2-amended violation ("nothing hard-codes a dark value that has a light sibling") in a form no guard can see. The live twin `GLASS.borderSoft` (~20 surfaces) is a hand-typed `colors.border` at 0.5 under a comment "kept as a literal because CSS has no alpha-on-hex" — `withAlpha` landed in this same PR precisely because CSS has no alpha-on-hex, and this PR's own `ExportCaseCard.tsx:128-130` fixes one member of the class by derivation. Held at LOW, not the lanes' MEDIUM, because plan U1.1's row explicitly derives `GLASS.gradientCard/gradientPanel/borderSoft/borderAccent` from `GLASS_TIER` ("so the existing ~40 importers keep working") — the derivation is scheduled one wave out, and `colors.border` is not going to move again inside that window.
Fix: delete `T.borderSoft` and `T.radius` (dead). Rewrite the `glass-tokens.ts:51` comment to say the literal is derived at U1.1 from `GLASS_TIER.dark.card.border` (the plan's row), not that CSS forbids derivation. Optional one-liner if the owner wants the window covered: `expect(GLASS.borderSoft).toBe('1px solid ' + withAlpha(colors.border, 0.5))` — note `withAlpha` emits the SPACED form, so pin the relation, not the bytes.
Owner: `ae5f52b4da850cd08` (U0 implementer A — U0.1 territory)

### F9 [LOW] `T.rowH: 44` is an unguarded duplicate of `touchTarget.min` — proposed deferral REFUSED; fix it here
Lanes: type-design (disclosed item, "awaiting the ledger"); U0.4 report §7 P-1 (deferral proposal); PR body "deliberate choices"
File: `features/demo/ui/inputs/input-theme.ts:44` (one reader: `inputs/TimeWheel.tsx:8`)
Issue: U0.4 moved the `touchFloor` anchor off `T.rowH` onto `scale.ts:67-68`, leaving the literal behind with nothing comparing it to `touchTarget.min` or the phone. The proposed deferral's reason — "a one-line edit from U0.4 into a file U2.1 rewrites is the cross-phase conflict §6.2 exists to avoid" — was true on the U0.4 package branch and is not true at the wave PR: U0.1 already rewrote `input-theme.ts` in this same PR, the fix round is one branch, and the ledger's bar is "a real reason to wait". A one-liner with no conflict does not clear it.
Fix: `rowH: touchTarget.min` (import from `../tokens/scale`; `as const` keeps the literal type `44`; the guard reads `scale.ts` directly so nothing else moves).
Owner: `ae5f52b4da850cd08` (U0 implementer A — U0.1 territory; `inputs/` file)

### F10 [LOW] The marketing device shell claims to COPY the demo shell's constants, and its screen backdrop no longer matches
Lanes: web — original label: LOW
File: `components/marketing/phone-frame.tsx:48` (`#0d1b2a`; docblock `:6-11` says the constants are copied from `features/demo/ui/PhoneFrame.tsx`, which now reads `colors.background` at `:60`)
Issue: the wall test (`components/marketing/__tests__/phone-frame.test.tsx:56-66`) guards the IMPORT, not the values, and `palette.test.ts`'s RETIRED scan is correctly rooted at `features/demo/ui`, so nothing notices the copy drifting. Cosmetically near-invisible (the slot is filled edge-to-edge by `<AppDemo/>`) — a false "copied from" comment is the cost. Same family as DIFF.md §4.6 (`app/css/style.css:27 --color-input: #2a4a6f` is a marketing token that happened to equal the demo's old `borderLight`; marketing is not a parity target — leave it).
Fix: either update the literal to `#002853` with a comment naming the demo line it mirrors, or state in the docblock that only geometry is mirrored and colour is marketing's own.
Owner: `ae5f52b4da850cd08` (U0 implementer A — the sweep's territory)

## Dropped / demoted lane findings

| Lane item | Lane · label | Disposition | Reason |
|---|---|---|---|
| Unanchored palette keys | typescript · MEDIUM | MERGED UP into F2 at HIGH | Same root as tests HIGH; my in-memory probe (67 rows / 0 drift) removes the "or ledger it" branch of the lane's fix — anchoring is free today, so a deferral would buy nothing. The lane's MEDIUM hedged on cost; the cost is zero. |
| Docblock says 35 rows | tests · LOW | folded into F2 | Same file, same fix commit. |
| RETIRED needle case-sensitive | silent-failures · MEDIUM | merged into F3 | Same root (raw-string compare in a literal scan); the tests lane's own fix line already said "apply the same treatment to the RETIRED loop". One helper closes both. |
| `before`-marker silent fallback | silent-failures · LOW (+ typescript out-of-lane) | folded into F4 | Same 12-line function, same owner, one commit. |
| `parseColor` `#rrggbbaa` pass-through | typescript · LOW (+ web out-of-lane `$` anchor) | folded into F6 | Same helper, same silent-degrade class. |
| hoist `withAlpha` in JSX | web · LOW | folded into F1 | F1's fix touches those exact lines. |
| borderSoft hand-typed (×2) / six alpha-derived tints / dead T keys | typescript · MEDIUM, web · MEDIUM, silent-failures · MEDIUM, type-design · LOW | merged and DEMOTED to LOW (F8) | Plan U1.1's row owns deriving `GLASS.borderSoft`/`borderAccent` from `GLASS_TIER` next wave; the other four tints are unchanged tokens D3 says not to sweep. What is actually W0's: two dead keys and a false comment. Four-lane convergence noted — the class is real, the schedule already answers it. |
| `#2B8CC1`-as-text crossed AA (4.66 → 3.94, 14 sites) | web · MEDIUM | LEDGERED — §89 | The lane itself asked for a row, not a sweep. D3 confines W0 to changed values; A27/A66 assign the adoption to U2/U6. Arithmetic independently reproduced (3.94 / 4.66). |
| `norm`'s `.trim()` is an equivalent mutation | tests · LOW | DROPPED | Lane's own verdict: "not worth a code change; worth knowing." Recorded here; no owner. |
| `Anchor.scheme` bare string + `'any'` sentinel | type-design · LOW | DROPPED | Cosmetic failure message on the one geometry anchor; no behavioural path. The `.mjs` cannot express the union (ledger §27 precedent, cited by the lane). |
| `T.rowH` deferral (U0.4 P-1) | disclosed | REFUSED → F9 | Reason does not hold at the wave PR (see F9). |
| Verification §4.1 depth inversion, §4.2 button hierarchy collapse, §4.4 `#05080d` overlays detached, §4.5 grid overlay stronger | verification · no label | NOT FINDINGS — sequencing | Each is a later row operating on a not-yet-moved token (A29 → U1.1, U2.2, A20 → U4, A10/A11 → U8.2). Recorded in Pipeline notes so the owner's device pass is not run against W0 alone. |
| Verification §4.3 `case-map/template.ts` frozen palette | verification · no label (+ foundation P-4) | LEDGERED — §92 | Plan §2 excludes the export's design system by name; the seat asked for a ruling — ruled as a ledger row with a concrete U8.4 trigger. |

**Rulings the authors asked for (two lanes each, aggregator concurs — recorded so no round re-litigates them):**
- `expect(anchors.length).toBe(33)` — **KEEP.** It is a set-size pin that fails on the meaningful mutation (deleting a key to dodge a red — tests probe H2 KILLED it), not a change-detector; its cost is a one-line edit at each closing act, which is the visible diff the plan wants. F2's membership assertion makes it a derived number, not a redundant one.
- The two camera-screen radial-gradient grounds (`MediaCaptureScreen.tsx:445`, `OcrCaptureScreen.tsx:495`) ported under D17 — **CORRECT, keep.** Matrix A1 enumerates both by `file:line` for U0; D17 freezes an enumerated list (iOS red, four scrim alphas, white) that does not include a radial inner stop, and the outer `#05080d` — the camera's own black — is untouched. The verification seat confirmed the port at source and that it is invisible behind the viewfinder.

## Owner routing summary

| Owner | Finding IDs |
|---|---|
| `ae5f52b4da850cd08` — U0 implementer A (U0.0–U0.3) | F1, F6, F7, F8, F9, F10 |
| `adff9eb9a7670742f` — U0.4 (guard) | F2, F4 |
| `aaa5c5ea7ea00825b` — U0.5 (contrast test, literal guards, `palette.test.ts`) | F3, F5 |

No orchestrator merge/seam code carries a finding (both merges were clean; nothing in the diff is orchestrator-authored).

## Ledger interaction

Triggers this diff satisfies: **§31** (P0.5 residuals — trigger "any actual demo restyle"): partially. Its two bare-colour bullets (`SyncStatusCard.tsx` `#2a4a6f`, `CaseMapPicker.tsx:131` `#1e3a5f`) were resolved by U0.1's sweep — noted in the row as `✅ PARTIALLY RESOLVED — PR #39`; its near-miss-gradient bullet is exactly plan U1.1/U1.2's normalisation, so the trigger is re-pointed there rather than filed TRIGGER-LAPSED against a scheduled row. No other row's trigger is touched (§15, §18, §28 checked by the silent-failures lane; my sweep of every token/palette/contrast-worded trigger found only §31).
Trigger-lapsed findings: none.
Rows written this round: **§89** (`#2B8CC1`-as-text AA regression, 4.66 → 3.94 — trigger U2 A66 / U6 adoption) · **§90** (dark-only CTA accent pair / `PrimaryButtonGradient.light` unowned — trigger U2.2, hard stop U8-exit light flip) · **§91** (`it.skipIf(!rnAvailable())` green-without-phone-repo — trigger: the first `.github/workflows/*.yml` that runs `pnpm test`) · **§92** (`case-map/template.ts` frozen palette — trigger U8.4 closing census, or any diff that opens the file) · **§93** (the eight unchanged high-frequency hexes not banned/swept — trigger: the commit that changes any one of them; U5.1 for `mapTokens.ts:58`).
Proposals NOT written, and why: U0.4 P-1 → refused, F9 (fix is a one-liner with no conflict at the wave PR). U0.4 P-3 (U3.1's rationale stale) and U0.5 P-2 (`header`/`elevated` in no ground stack; U1.4 puts text on `header`) are plan-brief corrections, not code non-fixes — orchestrator propagates at merge (listed below). U0.5 P-4: foundation P-3 (`DeleteConfirmationModal.tsx:152`) was resolved inside this PR (`18816dd`) and never reached the ledger — nothing to strike. Note: matrix line 550 pre-cites "`deferred.md` §89" for a hypothetical D5 fifth ceiling; §-numbers are not reserved (there is one writer), so §89 is taken by this round and that reference must be re-pointed if the D5 ruling ever lands.

## Agent IDs

| Seat | Agent ID | Source |
|---|---|---|
| typescript lane | `a4c5c572ffccfbfd2` | orchestrator dispatch record (r1) |
| web lane | `a8c0513e7045e143e` | orchestrator dispatch record (r1) |
| tests lane | `a612a9b18fb01a882` | orchestrator dispatch record (r1) |
| silent-failures lane | `a0eee46d047065bbd` | orchestrator dispatch record (r1) |
| type-design lane | `ae371985d5c932c30` | orchestrator dispatch record (r1) |
| verification seat | `ae2b8ca4003b5eb41` | orchestrator dispatch record (r1) |
| aggregator (this seat) | `a0a927cee97a72c8d` | orchestrator dispatch record (r1); fresh seat this build, warm from here |
| U0 implementer A | `ae5f52b4da850cd08` | brief |
| U0.4 implementer | `adff9eb9a7670742f` | brief |
| U0.5 implementer | `aaa5c5ea7ea00825b` | brief |

IDs supplied by the orchestrator after r1 — none of the lane files carries its own agent ID. Ask lanes to print their ID in the lane-file header from W1 on (pipeline note).

## Pipeline notes

- **Cross-lane confirmations (strong signal):** F2 — typescript and tests independently probed the guard's key set from two angles (membership swap; unanchored key re-base) and both SURVIVED. F3 — tests and silent-failures hit the two literal scans on two normalisation axes. F6 — type-design and silent-failures found the two silent arms of the same helper from opposite ends (type arity vs breadcrumb). F8 — FOUR lanes converged on the hand-typed alpha tints; demoted only because the plan already schedules the derivation. F1's arithmetic was reproduced by me to the second decimal.
- **The one severity dispute** (typescript MEDIUM vs tests HIGH on the 17 unanchored keys) was settled empirically: my in-memory extension of `PALETTE_KEYS` ran the real guard at 67 rows / 0 drift / 0 parse-failed with only `onPrimary`/`onError` scheme-invariant. The "cost of anchoring now" argument that held the typescript lane at MEDIUM has no cost behind it.
- **Lane quality this round:** every HIGH survived spot-check at source; no manufactured finding among 25 raw items. All four probing lanes cut their own worktrees, restored byte-identically and tore down with the script's proof line. The tests lane's seven re-verifications of the authors' claimed kills (after the U0.4 report disclosed a false-KILL runner) all reproduced — the authors' probe figures are trustworthy.
- **Coverage gaps:** no lane measured accent-as-MARK contrast structurally (F1 was found by the web lane's own arithmetic; every accent row in `palette-contrast.test.ts` is `it.todo` measuring white ON the stops). Every lane left the `it.todo` rows to their owners (all 15 verified genuinely blocked by the tests lane — none landable).
- **Noise patterns:** none worth a persona edit. Three LOWs dropped (equivalent mutation, cosmetic message, hoist) — the lanes flagged them honestly as marginal.
- **Contract hygiene:** the web lane reports its OWN session-scratchpad file (`.../scratchpad/c.mjs`) came back holding a different agent's contrast-table output — the scratchpad is evidently shared across concurrently-running lanes this round. Nothing was lost (the lane re-ran under unique filenames) and no lane file was affected. Orchestrator: give each lane a per-lane subdirectory or unique-prefixed filenames in the scratchpad brief. No lane found foreign content in its lane file; no stray probe files in the review directory.
- **Verification seat sequencing notes (not findings, for the owner's device pass):** W0 in isolation reads WORSE than master on the single most repeated surface — cards composite darker than the lightened page (depth inverted) until U1.1 re-bases `GLASS.gradientCard` (A29); primary/secondary buttons have collapsed to near-equal value until U2.2; the `#05080d` overlay family is now a hard cut (A20 → U4); the grid overlay reads stronger (A10/A11 → U8.2). Do not run the owner's device pass against W0 alone.
- **Plan / matrix corrections owed (orchestrator, at merge):** (1) U3.1's row says all four status keys "have no web-side token before this package" — `success`/`warning` exist since U0.1 (U0.4 P-3; interacts with F2). (2) U2.2's row gains `PrimaryButtonGradient.light` + rows 12L/13L + the two light gradient anchors (§90). (3) U8.4's row gains the `case-map/template.ts` decision (§92). (4) U1.4's brief must add `header`/`elevated` ground stacks to the contrast contract before wizard titles land on `header` (U0.5 P-2). (5) The U0.4 report §8 items 1–8 and U0.5 §8 corrections — the typescript lane spot-checked three and found them accurate. (6) Matrix line 550's forward reference to "§89" is now stale.
- **Gate 2 wording:** U0.5 report §10(a) — phase gate §6.6-2 ("the ported contrast test — green") is satisfiable by 4 live rows over 15 todos; two lanes concur it is a plan-wording problem. The honest shape is a phase-exit todo-subset check owned by the verification seat; belongs in the plan, not the test file.
