# Vetted Review — W1 (phase U1, PR #40 `feat/uiparity-w1` @ `28e7993` vs `feat/uiparity-u0`) — Round 1

**Verdict:** REVISE
**Lanes read:** typescript 0/1/0/2 REVISE · web 0/1/1/0 REVISE · tests 0/0/2/0 APPROVE-wc · silent-failures 0/2/0/1 REVISE · type-design 0/0/3/1 APPROVE-wc · verification seat (`_captures/w1/DIFF.md`, no labels — 71 / 15 / 31 of 117 pairs; W0 §4.1 depth inversion and §4.2 button collapse both CLOSED; 0 regressions; 2 items)
**After dedupe:** 0 critical · 1 high · 5 medium · 3 low  (16 raw lane items + 2 verification items + 8 report deferral proposals → 9 findings, 5 ledger rows)
**Unsettled (operator escalation):** 0
**Aggregator:** warm — same seat as W0 (`a0a927cee97a72c8d`). F-IDs continue from W0 (F1–F13 closed at PR #39); this round is F14–F22. Skeleton written to disk first, then this document.

Cold gates at `28e7993`, reproduced by three lanes and the integrator: tsc 0 · 272 files / 3,562 passed | 10 todo · guard 115/115 (32 palette + 24 tier keys × both halves + 3) · `/demo` 107 kB.

## Unsettled — for the operator (never affects the verdict)

None. Three disputes settled at source: (1) severity of the tier-totals finding (sf HIGH vs tests/td MEDIUM) — MEDIUM, see F16; (2) web's `glassCardNested`-needs-a-shadow MEDIUM — refuted at the phone's own source (`Colors.ts:376-378` draws `nestedCard` raised WITHOUT a shadow by design because its iOS shadow is dead), folded into F19 as a docblock touch-point; (3) the integrator's "two standing ledger rows from W0 (PR-1 → U2.2, PR-3 → U7.2)" — those rows were never written: both proposals were refused in `w0/VETTED-r1-delta.md` (PR-3 became F12 and is fixed). Corrected here.

## Findings

### F14 [HIGH] The card recipe's documented escape hatch for re-tinting a card's sides does not work — the prescribed form paints the side tint on the lit top edge on first render, silently
Lanes: web — original label: HIGH; typescript LOW ("boxShadow fusion is a second silent-wipe vector, undocumented") folded in as a touch-point
File: `features/demo/ui/glass-tokens.ts:25-27` and `:129-131` (the docblock: "A consumer that must re-tint the sides sets `borderColor` and then re-sets `borderTopColor`"); `:141` / `:178` (the fused `boxShadow` keys)
Issue: every one of the 22 consumers SPREADS the fragment, and under spread a duplicate key keeps the FIRST key's position with the last value — so the "re-set" `borderTopColor` collapses back into the spread's slot ahead of `borderColor`, and `borderColor` (a four-side shorthand) then erases it. Web lane MEASURED it in jsdom/React 19.2: prescribed form → first paint `rgb(1, 1, 1)` where `rgba(184, 212, 240, 0.08)` was expected. The same probe found the update-path half the docblock does not mention: a CONDITIONAL `border`/`borderColor` on a fragment carrying `borderTopColor` is right on first paint and wrong on the next render (React writes only changed keys; stderr warns "Updating a style property during rerender (border) when a conflicting property is set (borderTopColor)"). No shipped consumer does either today (`glass-card-recipe.test.tsx:225` loops all nine `glassCard` consumers and would catch it) — the severity is the use-day: this sentence is the standing instruction for U2.4 / U4.1 / U5.1, named as this seam's consumers in the module header, and the coordinator has already had to warn the W2 seats out-of-band. Touch-point: `glassCard.boxShadow` fuses the tier inset (A32) with `GLASS.shadowCard` (A44), so a consumer writing `boxShadow` after the spread drops the inset highlight too — same class, undocumented (ts probe 5: KILLED where covered).
Fix: replace the sentence at both sites with a form that works and say why — destructure (`const { borderTopColor, ...base } = glassCard` then `{ ...base, borderColor: X, borderTopColor }`) or set the edge through a key the fragment does not carry (`borderTop: '1px solid <h>'`, which appends after `borderColor`). Add the update-path rule: a conditional side tint must re-declare the edge in the same object every render. Add one clause for `boxShadow`: overriding it after the spread drops the inset — re-compose or do not override. New consumers must be added to `CONSUMERS` in `glass-card-recipe.test.tsx:211`, which is the pin that observes this. No new test prescribed.
Owner: `aec4149c990c8d0ef` (U1.2/U1.3 seat — `glass-tokens.ts` owner this round)

### F15 [MEDIUM] W0's F7 (`satisfies typeof colors.primaryDark`) binds a DARK-ONLY constant to the CONSUMED scheme — flipping `scheme = 'light'`, the one-site change three docblocks in this diff promise, is a hard TS1360 in `glass-tokens.ts`
Lanes: typescript — original label: HIGH (DEMOTED, see Dropped / demoted)
File: `features/demo/ui/glass-tokens.ts:52` (`const ACCENT_FROM = '#1F6B99' satisfies typeof colors.primaryDark`); `features/demo/ui/tokens/palette.ts:185-186` (`export const scheme = 'dark' satisfies ColorScheme; export const colors = palette[scheme]`)
Issue: `colors` is `palette[scheme]`, so `typeof colors.primaryDark` is `'#1F6B99'` only while `scheme === 'dark'`; `ACCENT_FROM` is `PrimaryButtonGradient.DARK`'s top stop by definition and the demo has no light pair (ledger §90). Typescript probe 1: `'dark' → 'light'` at `palette.ts:185` → `tsc` exit 2, `glass-tokens.ts(52,31): TS1360: Type '"#1F6B99"' does not satisfy the expected type '"#1e40af"'`. The claim it falsifies is asserted at `palette.ts:177`, `glass-tiers.ts:28` and `glass-tokens.ts:58-60`, and W1 spent real budget on it (48 light tier values nothing renders; `scheme` exported for exactly this). Confirmed by reading, no re-run needed. Probe 1b verified the fix both ways: with `typeof palette.dark.primaryDark` the flip compiles AND the one-sided `primaryDark` re-base still dies (TS1360) — F7's kill is preserved. Precedent for this seat: I accepted F7's shape in W0; the lane's correction is right and the shape was wrong for the flip.
Fix: one token — `satisfies typeof palette.dark.primaryDark`, importing `palette` beside `colors`. Extend the `:33-38` comment by one clause (dark by definition, scheme-independent so it guards after the flip). `ACCENT_TO` stays plain.
Owner: `aec4149c990c8d0ef` (U1.2/U1.3 seat — `glass-tokens.ts` owner this round; F7's author was U0 seat A, but one writer per file wins)

### F16 [MEDIUM] Tier membership in the drift guard is pinned by three hand-typed counts, not against the module — the exact shape W0/F2 removed for the palette, three lines below the comment explaining why
Lanes: silent-failures — original label: HIGH ("a whole tier drops out of the guard by editing two numbers — SURVIVED"); tests — original label: MEDIUM ("a seventh tier lands, guard unchanged — SURVIVED"); type-design — original label: MEDIUM ("addition killed only by a different gate"); typescript probe 3 (context: the count-preserving SWAP is KILLED by the both-halves loop, "not the W0 hole")
File: `.design-sync/check-rn-parity.mjs:353-354` (`TIER_KEYS`, `TIER_PARTS`); `features/demo/ui/inputs/__tests__/rn-token-parity.test.ts:151-158` (`.toBe(6)`, `.toBe(4)`, `.toBe(24)`)
Issue: `PALETTE_KEYS` is held to `Object.keys(palette.dark)` (`:137-139`); `TIER_KEYS` is held to nothing outside itself. Three lanes probed three shapes: a swap (`recessed→card`) is KILLED — the both-halves loop doubles as a duplicate detector (ts); an ADDITION (a seventh tier in `GlassVariant` + both halves) leaves the guard at 115 and the parity file green (tests, td — td's was ultimately killed by `glass-tiers.test.ts`'s whole-object shape pin, which only forces you to NOTICE the tier; after the house-rule in-place rewrite of that pin, nothing requires the anchor); a SHRINK that drops `recessed` and edits the two counts → standalone "107 rows OK", suite 15/15 green, eight rows silently gone (sf). Held at MEDIUM, not sf's HIGH: all 24 tier keys are anchored today (no live gap — the difference from W0/F2, where 17 keys were unanchored); the surviving shapes are a future addition no scheduled package makes and a shrink that edits the test's own literals; the fix is one line with the precedent in the same file.
Fix: in the tier case, import `GLASS_TIER` and assert membership — `expect([...TIER_KEYS].sort()).toEqual(Object.keys(GLASS_TIER.dark).sort())` — and make the `innerShadow` exclusion a NAME rather than the difference between 4 and 5: `expect([...TIER_PARTS_AS_TIER_FIELDS, ...UNANCHORED].sort()).toEqual(Object.keys(GLASS_TIER.dark.card).sort())` with `UNANCHORED = ['innerShadow']` (the `SCHEME_INVARIANT` idiom the file already uses — note `TIER_PARTS` spells the two stops as `gradientTop`/`gradientBot` where the module has one `gradient` tuple, so map before comparing). Delete the three `.toBe(...)` literals. PRESCRIPTION-UNVERIFIED (no lane executed the prescribed pin; re-run sf's shrink probe and td's addition probe against it).
Owner: `ac9a1a0c98322500a` (U1.1 seat — guard + tier anchors)

### F17 [MEDIUM] With both tier scopes stuck on one half, the STANDALONE guard prints "all 115 anchor rows match" and exits 0 while comparing light to light — only the vitest structural pin sees it, so the two entry points disagree
Lanes: silent-failures — original label: HIGH (DEMOTED, see Dropped / demoted)
File: `.design-sync/check-rn-parity.mjs:231-247` (`rnTierScope` / `webTierScope`, twins sharing one docblock), `:466-469` (`checkParity` returns only `drift` + `parseFailed`), `:479-499` (the standalone reporter); the pin that does catch it: `rn-token-parity.test.ts:196-230`
Issue: sf probe (B): both twins flattened to one level → standalone `✓ all 115 anchor rows match`, EXIT 0; vitest EXIT 1 (3 failed). Control (A, one side only): both entry points red. `drift` is `a.rn !== a.web` and light equals light; the light≠dark structural pin lives only in the test. The module header advertises the standalone as authoritative ("exit 1 on drift or mismatch") and the PR body quotes it as verification evidence; the `webTierScope` docblock names this exact trap and the module does not self-check for it. Held at MEDIUM: `pnpm test` — the actual phase gate — kills the mutation; the standalone is the secondary entry point, so this is a misleading green on the weaker path, not a gate failure.
Fix: compute `stuck` inside `checkParity()` — rows whose light and dark reads are identical on a side, minus the by-name `{onPrimary, onError}` exclusion — return it beside `drift` and `parseFailed`, exit 1 on non-empty and print it like the other two lists; the vitest case then asserts the guard's own result instead of re-deriving it. Keep the `SCHEME_INVARIANT` set in one place (export it from the guard).
Owner: `ac9a1a0c98322500a` (U1.1 seat)

### F18 [MEDIUM] Plan §9 clause 12's "one-site scheme flip" is held by prose only: `GLASS_TIER.dark` in either consumer is invisible to every gate (two SURVIVED probes), the deferral that parked it named a trigger that fired inside this PR, and `header-chrome.test.tsx:24` claims a coverage it does not have
Lanes: tests — original label: MEDIUM · TRIGGER-LAPSED (on U1.4 report §6 D-2, a PROPOSED deferral never written to the ledger — treated as a refused deferral, not a lapsed §); type-design — original label: MEDIUM ("no gate on direct-half access")
File: `features/demo/ui/controls/header-chrome.ts:63` and `features/demo/ui/glass-tokens.ts:62` (the two `GLASS_TIER[scheme]` sites — completeness sweep: production has exactly these two; every other direct-half access is a test and legitimately so); `features/demo/ui/controls/__tests__/header-chrome.test.tsx:24` (docblock: "reading `GLASS_TIER.dark` directly reddens the file" — half true); the scan home: `features/demo/ui/__tests__/glass-tokens.test.ts` (`sourceFiles(UI_ROOT)` walk, `TOKEN_MODULES` allow-list at `:47-52`)
Issue: tests probes A1/A2: `GLASS_TIER[scheme]` → `GLASS_TIER.dark` at each site — SURVIVED (12 passed; 28 passed), cold tsc 0 (no `noUnusedLocals`, so the orphaned `scheme` import does not fail either). Control RV2 (wrong TIER on the same line) is KILLED with 4 failures, so the docblock's "sourcing a bar from `card`" half is true and its `GLASS_TIER.dark` half is not. While `scheme === 'dark'` the two expressions are the same object, so no behavioural pin can ever tell them apart — only a source scan can, and the repo has exactly one (`glass-tokens.test.ts`'s walk). U1.4's D-2 deferred the scan to "the next commit that touches `glass-tokens.test.ts`'s scan block — U1.2's `BANNED` rewrite"; that commit is `74855c1`, in this PR. The proposal was written before U1.2 merged (sequencing, not bad faith); its own trigger condition has occurred, so it is a finding. `glass-tokens.ts:19` now actively directs new code to "reach for `GLASS_TIER[scheme].<tier>` directly", and six later packages index this module.
Fix: the author's own four lines beside the existing walk in `glass-tokens.test.ts` — `it('no consumer hard-codes a scheme half (§9 clause 12)')` filtering `sourceFiles(UI_ROOT)` on `/GLASS_TIER\s*\.\s*(dark|light)|palette\s*\.\s*(dark|light)/` and expecting `[]` (`sourceFiles` already skips `__tests__` and `TOKEN_MODULES`, which is right — `glass-tiers.ts` declares both halves and `palette-contrast.test.ts` legitimately reads both). Then rewrite `header-chrome.test.tsx:24` to claim only what it delivers. PRESCRIPTION-UNVERIFIED (nobody ran the scan; re-run tests probes A1/A2 against it).
Owner: `a9f135565ce43133b` (U1.4 seat — D-2's author; `glass-tokens.test.ts` and `header-chrome*` are its files this fix round)

### F19 [MEDIUM] `GLASS.shadowCard` hard-codes `Layout.shadow.card.dark` with no scheme indirection — a D2-amended violation whose light sibling EXISTS at the phone's source — and the wave's shadow story is otherwise unreconciled in writing
Lanes: type-design — original label: MEDIUM; web — original label: MEDIUM ("`glassCardNested` drops an elevation shadow the phone's `Card` applies to every variant") — folded in as a docblock touch-point after refutation at source
File: `features/demo/ui/glass-tokens.ts:104` (`shadowCard: '0 4px 8px rgba(0,0,0,0.15)'`), consumed at `:142` (`glassCard.boxShadow`) and `screens/export/ExportCaseCard.tsx:138`; `:160-167` (`glassCardNested` docblock, "NO ELEVATION SHADOW, deliberately … A55 names none")
Issue: D2-amended, verbatim: "Nothing hard-codes a dark value that has a light sibling." Aggregator read the phone source: `Layout.ts:122-136` — `shadow.card.light = { shadowColor 'rgba(30, 58, 138, 0.18)', offset {0,3}, opacity 1, radius 8 }` vs `.dark = { '#000', {0,4}, 0.15, 8 }` — materially different (tinted, not black; offset 3, not 4). The comment at `:96-99` names the dark half as such. On the flip day every card keeps a black drop under a white surface, and (correctly disclosed at `:100-102`) no anchor will ever notice. `shadow.card` appears once in the plan (U1.2's row), so no later package owns the light half. Web's shadow half, adjudicated at the phone's source rather than the inventory: `Colors.ts:376-378` (the `nestedCard` docblock) says "A defined border plus a genuinely lit top edge is how a raised panel is drawn WITHOUT a shadow, which matters here because the iOS shadow on this component is dead … and repairing it is held" — so U1.3's no-shadow nested tier IS the phone's design, and A55's omission is deliberate; and the `card` shadow follows matrix A44/A54 as owner-ratified even though the phone's iOS shadow is currently dead. Both halves are decided; neither is written down where the next reader looks.
Fix: give `shadowCard` the wave's own idiom — `{ light: '0 3px 8px rgba(30,58,138,0.18)', dark: '0 4px 8px rgba(0,0,0,0.15)' } as const` read via `[scheme]` (three lines; `Layout.ts:122-136` cited). In the `glassCardNested` docblock, cite `Colors.ts:376-378` as the phone's own reason (drawn without a shadow by design; iOS shadow dead), and in the `shadowCard` comment state that A44/A54 port the phone's INTENDED card shadow although its iOS rendering is currently dead. The unanchored-shadow class goes to the ledger (§95), not to code.
Owner: `aec4149c990c8d0ef` (U1.2/U1.3 seat)

### F20 [LOW] `header-chrome.ts` annotates `: CSSProperties` where its sibling in the same PR uses `as const satisfies CSSProperties` — literal types are erased, and the file's own test already pays an `as string` cast for it
Lanes: typescript — original label: LOW; type-design — original label: LOW (independent, same finding — measured: `glassHeaderBar.background` is `Background<string|number> | undefined`, while all four `GLASS` derivations keep their literals)
File: `features/demo/ui/controls/header-chrome.ts:72, :84, :100`; the cast at `controls/__tests__/header-chrome.test.tsx:169`
Issue: excess-property checking still applies, so no invalid state is admitted; what is lost is the derivation being visible in the type (the `satisfies typeof …` device F7/F15 rely on is unavailable against the header recipe), and commit `7a3a75f` ("type the style pins for the cold typecheck") is that friction made visible.
Fix: `} as const satisfies CSSProperties` on all three (`glassWizardHeaderBar` spreads `glassHeaderBar`, so they move together); drop the `as string` at `header-chrome.test.tsx:169`.
Owner: `a9f135565ce43133b` (U1.4 seat)

### F21 [LOW] `readStop` reads the first two stops of a longer tuple and reports OK on the truncation
Lanes: silent-failures — original label: LOW
File: `.design-sync/check-rn-parity.mjs:196-200` (the tuple regex is unbounded on the right)
Issue: `gradient: ['a','b','c']` matches and compares only `a`,`b`; a one-stop tuple correctly throws (PARSE-FAILED) and the web type forbids a third stop, but a phone-side three-stop gradient would report OK against a gradient the demo cannot render. Truncation is the one malformed shape the reader accepts silently.
Fix: append `\s*\]` to the pattern so a longer tuple fails to match and lands as PARSE-FAILED. One character class; both current callers pass 2-tuples.
Owner: `ac9a1a0c98322500a` (U1.1 seat)

### F22 [LOW] The two header-tier values U1.4 first wrote into a recipe are not in `BANNED` — U1.4's own D-1 named "U1.2's `BANNED` rewrite commit in this wave" as trigger (a), and that commit (`74855c1`) is in this PR
Lanes: aggregator (from U1.4 report §6 D-1 — a proposed deferral whose first trigger fired before the PR closed; silent-failures independently confirmed both literals have zero occurrences under `ui/` outside `tokens/glass-tiers.ts`)
File: `features/demo/ui/__tests__/glass-tokens.test.ts` `BANNED` (`:68-133`); the values: `header.border` = `sheet.border` = `rgba(28,78,132,0.6)` and `header.highlightTop` = `rgba(153,186,221,0.1)`
Issue: per the list's own rule the package that first writes a tier value into a recipe bans it; U1.4 deferred only because `glass-tokens.test.ts` was U1.2's file in the same window. That window is closed, F3's `norm()` is merged (so the entries are no longer "half-blind" to spaced pastes — the report's second reason is also moot), and U4.1 writes the identical `sheet.border` next wave.
Fix: the two rows the report already spells: `['header/sheet border', 'rgba(28,78,132,0.6)']`, `['header highlightTop', 'rgba(153,186,221,0.1)']`. Same commit as F18 (same file, same owner).
Owner: `a9f135565ce43133b` (U1.4 seat)

## Dropped / demoted lane findings

| Lane item | Lane · label | Disposition | Reason |
|---|---|---|---|
| `scheme='light'` does not compile | typescript · HIGH | DEMOTED → F15 MEDIUM | The break is LOUD (TS1360 at compile time, pointing at the line), the fix is one token probed both ways by the lane, and the use-day (the U8-exit flip, plan §9 clause 12) has a scheduled scratch-worktree verification — that day has a reviewer. Rubric: real issue, limited impact. Precedent for this seat: a loud compile-time break of a scheduled-day contract with a one-token fix is MEDIUM; a silent survivor (W0 F11) is HIGH. The contract violation is real and is fixed this round regardless. |
| Standalone guard blind to a stuck scope | silent-failures · HIGH | DEMOTED → F17 MEDIUM | `pnpm test` — the phase gate — kills the mutation (3 failed); the survivor is on the secondary entry point only. Misleading green on the weaker path, fixed by sharing the check, not a gate failure. |
| Tier totals hand-typed | silent-failures · HIGH · tests MEDIUM · type-design MEDIUM | MERGED → F16 MEDIUM | Four-lane convergence; no live gap (all 24 keys anchored — the difference from W0/F2's 17 unanchored); the count-preserving swap is killed (ts probe 3); the survivors are a future addition (caught by the shape pin until rewritten) and a shrink that edits the test's own literals. One-line fix, precedent in the same file. |
| `glassCardNested` lacks the shadow `Card` applies to every variant; dark card shadow is "dead" on the phone | web · MEDIUM | REFUTED at source, folded into F19 as a docblock touch-point | `Colors.ts:376-378` (phone): nested is drawn raised WITHOUT a shadow by design because the iOS shadow is dead — U1.3 matches the phone's design and rendered state; matrix A44/A54 (owner-ratified) port the card shadow by intent. Both decided; the finding's live remainder is "write it down", which F19 carries. |
| `boxShadow` fusion is a second undocumented wipe vector | typescript · LOW | folded into F14 | Same docblock, same owner, same commit. |
| `header-chrome.ts` `: CSSProperties` | type-design · LOW | merged into F20 | Independent identification of the same finding, with the type-probe evidence. |
| Stale "whitespace-blind" comment at `glass-tokens.test.ts:96-101` | silent-failures · out-of-lane | not filed | Lines 96-101 at `28e7993` describe the A36 rewrite, not the whitespace limit; the `norm` docblock at `:33-43` is current (F3). Could not confirm at the cited lines. |
| Verification §4.1 header/body ≈4 RGB | verification · no label | not a finding — device pass | Parity with `Colors.ts:391-396`; DIFF.md §5 checkpoint 3 puts it on the owner's D1 pass. |
| Verification §4.2 `colors.modal` has no consumer and no adopter row | verification · no label | LEDGERED — §94 | Trigger U4.2 is queued in W2 and its row opens `_shared.tsx`'s dialog shell. Confirmed: `grep colors.modal` under `features/demo` (non-test) = 0 hits. |
| Diagonal card variant (8 surfaces incl. Cases list + Dashboard) did not get the four-part composition | U1.2 report D-1 (+ typescript out-of-lane "is it intended scope?") | LEDGERED — §96 | A real gap on the two entry screens, drawn by the plan (U1.2's row = the `glassCard` consumers; D11 keeps `gradientCardDiag` as a bare string). Files owned by U3.3/U3.4/U5.4; concrete trigger (U3.3's `DemoErrorBoundary` card re-base) plus the D1 device-pass checkpoint 1 as backstop. |
| Bundle log missing at the brief's path | web · out-of-lane | resolved | The integrator's cold `pnpm build` at `28e7993` reports `/demo` 107 kB; `_captures/w1-assembly-gates.log.build` was never written — orchestrator: fix the brief's path or the capture step. |

**Report deferral proposals — dispositions (default FIX or REFUTE; five rows written):**
- U1.1 D-1 (`flattenOver` per-fold rounding, ≤0.32 ΔE vs the phone's floats) → **§98** (numeric trigger; sf concurs it is bounded and not a defect).
- U1.1 D-2 (whitespace-blind `BANNED` scan) → **RESOLVED**, not deferred — W0/F3 `696f3bb`, merged into this branch at `b56b358`; nothing to write.
- U1.1 D-3 (`innerShadow` ×12 has one gate) + U1.2 D-3 (`Layout.shadow` has no drift anchor on either side) → **§95**, one row for the class (the guard cannot read a native shadow prop as a flat value; trigger U4's `shadow.dialog`/`sheet`).
- U1.2 D-1 (diagonal card variant) → **§96**.
- U1.3 D-2 (`nestedCard.border` cannot be banned while `BootSequence.tsx:36` spells it — confirmed at source) → **§97** (trigger U8.1).
- U1.4 D-1 (two header-tier `BANNED` rows) → **REFUSED — trigger (a) fired in this PR → F22**.
- U1.4 D-2 (`GLASS_TIER.dark` uncatchable) → **REFUSED — trigger fired in this PR → F18**.
- Integrator "standing rows PR-1/PR-3" → **do not exist**; record corrected above.

## Owner routing summary (one writer per file)

| Owner | Finding IDs | Files |
|---|---|---|
| `aec4149c990c8d0ef` — U1.2/U1.3 seat | F14, F15, F19 | `features/demo/ui/glass-tokens.ts` only |
| `ac9a1a0c98322500a` — U1.1 seat | F16, F17, F21 | `.design-sync/check-rn-parity.mjs`, `inputs/__tests__/rn-token-parity.test.ts` |
| `a9f135565ce43133b` — U1.4 seat | F18, F20, F22 | `__tests__/glass-tokens.test.ts`, `controls/header-chrome.ts`, `controls/__tests__/header-chrome.test.tsx` |

Three seats, three disjoint file sets — no integrator conflict is possible in this fix round. W0's seat A is not dispatched (F15 lands in a file the U1.2 seat holds).

## Lanes to resume for the fix-delta

All five, each scoped to its own findings plus the fix diff: web (F14), typescript (F15, F20; F14's `boxShadow` clause), tests (F16, F18 — re-run probes A1/A2/B), silent-failures (F16, F17, F21 — re-run the shrink and flatten probes), type-design (F16, F18, F19, F20). Not a resume-nobody round.

## Ledger interaction

Triggers this diff satisfies: **none** in the ledger (§89–§93 all later-package; §31 unchanged). Two PROPOSED deferrals had their triggers fire inside this PR and were refused as findings instead (F18, F22) — that is the reviewer-contract §4 mechanism working on a proposal before it reached the ledger.
Trigger-lapsed findings: none against a written §.
Rows written this round: **§94** (`colors.modal` has no consumer and no adopting row — trigger U4.2) · **§95** (hand-ported shadow values — `innerShadow` ×12, `Layout.shadow.card` — have no drift anchor on either side; only literal shape pins — trigger U4's `shadow.dialog`/`sheet`, or the phone publishing a `boxShadow` string) · **§96** (the diagonal card variant's eight surfaces, including the two entry screens, still render a two-part card — trigger U3.3's `DemoErrorBoundary` re-base; backstop D1 device-pass checkpoint 1) · **§97** (`nestedCard.border`'s composed form cannot be banned while `BootSequence.tsx:36` spells it — trigger U8.1) · **§98** (`flattenOver` rounds per fold, ≤0.32 ΔE from the phone's float compositing — trigger: any contrast row within 0.35 ΔE / 0.03 ratio of its bound, or U2.4's recessed rows).

## Agent IDs

| Seat | Agent ID | Note |
|---|---|---|
| typescript / web / tests / silent-failures / type-design lanes | `a4c5c572ffccfbfd2` / `a8c0513e7045e143e` / `a612a9b18fb01a882` / `a0eee46d047065bbd` / `ae371985d5c932c30` | assumed WARM from W0 (each lane file cites its own W0 findings); orchestrator: confirm or correct before committing |
| verification seat | `ae2b8ca4003b5eb41` | assumed warm from W0 |
| aggregator (this seat) | `a0a927cee97a72c8d` | warm |
| U1.1 seat | `ac9a1a0c98322500a` | brief |
| U1.2/U1.3 seat | `aec4149c990c8d0ef` | brief |
| U1.4 seat | `a9f135565ce43133b` | brief |
| U0 seat A (not dispatched this round) | `ae5f52b4da850cd08` | brief |

Lane files still do not print their own agent ID (W0 pipeline note repeated).

## Pipeline notes

- **Cross-lane confirmations:** F16 — FOUR lanes (three probes of three shapes; the ts lane's "not the W0 hole" swap probe is the negative control that bounds the severity). F18 — tests and type-design from opposite ends (SURVIVED probes vs the missing gate). F20 — ts and td independently, td with a type-probe. F15/F18 together say the same thing about plan §9 clause 12: the contract is asserted in five docblocks and enforced by nothing; after this round it is enforced by a compile-time identity that survives the flip and a source scan.
- **Adjudication precedent recorded for the successor:** (1) loud compile-time break of a scheduled-day contract with a one-token fix = MEDIUM; silent survivor = HIGH. (2) A survivor only on the secondary entry point when the primary gate kills it = MEDIUM. (3) Hand-typed cardinality on a fully-anchored list = MEDIUM; on a list with live unanchored keys = HIGH (W0 F2). (4) Where the inventory and the phone SOURCE disagree, the source governs — F19's nested-shadow half was settled by `Colors.ts:376-378`, not by §2.A's prose.
- **My own W0 ruling corrected:** F7's `satisfies typeof colors.primaryDark` was accepted at W0 fix-delta; it binds to the consumed scheme and breaks the flip. The lane that caught it is the lane that verified F7 — a warm seat re-questioning what it approved is exactly the value of warmth.
- **Integrator record error:** `INTEGRATION-u1-assembly.md` §7 cites "two standing rows from `../w0/INTEGRATION-r1.md` (PR-3 → U7.2; PR-1 → U2.2)". Neither was ever written — both were refused in `w0/VETTED-r1-delta.md`, PR-3 became F12 (fixed at `b4de0a1`). Integrators should take ledger state from the VETTED docs and `deferred.md`, never from their own prior proposals.
- **Lane quality:** the tests lane recorded its own mis-scoped probe (P13r) and corrected it rather than filing a tautology; the web lane cut a real React 19 probe for a CSS-semantics claim instead of reasoning about it; the U1.2/U1.3 and U1.4 seats each caught and fixed their own tautological pins pre-push (three and one). Zero manufactured findings among 16 raw items.
- **Verification seat:** W1's before-set is W0's `after-fixed/` (`15e5a6f`) — the merge-gate re-cut I asked for in W0 was done and reused; the wave's headline (depth inversion closed, buttons re-separated) is measured off pixels. Device-pass checkpoints 1–8 in DIFF.md §5 are the owner's D1 list; checkpoint 3 (header/body ≈4 RGB) is the thinnest margin.
- **Plan / matrix corrections owed (orchestrator, at merge):** the 16 refutations across the three reports (§9 of each), notably plan §4.3's "override `borderColor`, never `border`" (wrong — `border-color` is a four-side shorthand; web and ts both probed it) and matrix row 82 / A37 (`SettingsNavBar` reads the ELEVATED tier and already matches byte-exact). Strike W0's owed item "U1.4 +`header`/`elevated` ground stacks": U1.4 §9(5) shows the extension is measurably inert (header+elevated min above the bound min in all eight rows; web reproduced to four decimals) — U0.5 P-2 is closed. Add §94–§98's named triggers to the U4.2, U4, U3.3, U8.1 and U2.4 rows.
