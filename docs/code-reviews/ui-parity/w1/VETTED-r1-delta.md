# Vetted Review — W1 (phase U1, PR #41 `feat/uiparity-w1`) — Round 1 FIX-DELTA @ `044578a`

**Verdict:** APPROVE with comments — one MEDIUM open (F23); recommended as a one-commit rider before merge, then a targeted delta (below)
**Fix diff:** `fc75577..044578a` (10 fix commits + 1 es5 follow-up + 3 package merges + the master carry `044578a`) · mapping comment on PR #41 · integrator `INTEGRATION-w0-carry.md` § master carry (m.1–m.4)
**Lanes read (delta sections):** typescript F15/F20/F14 FIXED, 1 new HIGH, REVISE · web F14/F19 FIXED (withdrew its own r1 remedy after re-probe), 0 new, APPROVE · tests F18/F16/F17 FIXED, F14 SOUND, F19 PARTIAL, 1 new MEDIUM, APPROVE-wc · silent-failures F17/F16/F21 FIXED, 1 new MEDIUM, APPROVE-wc · type-design F16/F19/F20 FIXED, F18 PARTIAL, 1 new MEDIUM, APPROVE-wc
**Prior findings:** 8 FIXED · 1 PARTIAL (F18 — remainder tracked as F23) · 0 UNFIXED
**New this round:** 0 critical · 0 high · 1 medium · 0 low — **F23** (four lanes, one defect; ts HIGH demoted, see below)
**Unsettled (operator escalation):** 0 verdict-bearing · 1 pending an external ruling (pipeline notes)
**Aggregator:** warm (`a0a927cee97a72c8d`). Skeleton written first, then this document.

Cold gates at `044578a` (integrator, cold cache; reproduced by ts and tests): tsc 0 · 272 files / 3,575 passed | 10 todo · guard 115/115 (drift 0 / parseFailed 0 / **stuck 0** — F17's new return) · `/demo` 107 kB · `rn-token-parity.test.ts` 18 passed, 0 skipped.

## Prior findings — status (my verification of each fix commit, then the lanes' re-checks)

| F-ID | r1 sev | Commit(s) | Status | Evidence |
|---|---|---|---|---|
| F14 | HIGH | `3c1eac3` | **FIXED** — prescription refined by the author, correctly | Docblock at both sites (`glass-tokens.ts:25-45`, `:143-152`) now prescribes the THREE SIDE LONGHANDS with no shorthand, names both broken forms, adds the `boxShadow` compose clause (ts's folded LOW) and the "add new consumers to `CONSUMERS`" rule. Five pins + two NEGATIVE CONTROLS in `glass-card-recipe.test.tsx:509-609`, exercising `rerender` (the update path). My r1 fix text ("destructure, re-declare the edge") was itself wrong on update — web re-probed all four forms and withdrew it; ts probe 3 shows the lit edge surviving the shipped form at a live consumer; tests judged the pins non-tautological (constants composed from `GLASS_TIER`, not retyped). |
| F15 | MEDIUM | `8d65308` | **FIXED** | `satisfies typeof palette.dark.primaryDark` (`glass-tokens.ts:77`), `palette` imported, comment rewritten naming the finding. ts probes 1a/1b and td: `scheme='light'` compiles (exit 0); one-sided `primaryDark` re-base still TS1360. |
| F16 | MEDIUM | `3c31600` + `e56c0f1` | **FIXED** — better than prescribed | `[...TIER_KEYS].sort()` vs `Object.keys(GLASS_TIER.dark)`; parts derived from `Object.keys(GLASS_TIER.dark.card)` with `UNANCHORED = ['innerShadow']` as a NAME (`rn-token-parity.test.ts:101-131`). Integrator relocated both pins into F11's ungated describe — correct, same two-local-lists shape. Killed phone-present AND phone-absent by tests (B-a/b/c), sf (shrink + 5th part), td (7th tier), integrator (F16a/F16b). The 5th-part probe nobody ran at r1 is now killed. `e56c0f1` `indexOf` dedupe is right for `target: es5`. |
| F17 | MEDIUM | `47a7f90` | **FIXED** | `stuck` computed in `checkParity()` (`check-rn-parity.mjs:483-509`), skipping PARSE-FAILED pairs and `SCHEME_INVARIANT` (now the guard's single export); CLI prints it and exits on `drift.length \|\| stuck.length` (`:544`); the vitest case asserts the guard's own `stuck` (`:249-278`). sf's r1 survivor (both twins flattened) → CLI EXIT 1 naming 48 pairs; tests probe C and integrator F17 concur. Vacuity checked by sf: the both-halves loops guarantee every key has a pair. |
| F18 | MEDIUM | `c0458b6` | **PARTIAL** → remainder **F23** | The scan exists and bites at both live consumers (ts 2a/2b, tests A-i/A-ii KILLED — including `glass-tokens.ts`, which my r1 prescription's `sourceFiles` skip-set would have EXEMPTED; the author's `SCHEME_DECLARERS` refinement is the substance). `stripComments` and the adjacency-scoped `typeof` carve-out both verified necessary and non-leaky (ts 2c, tests A-iii, sf). `header-chrome.test.tsx:24` docblock corrected. But `SCHEME_HALF` is a two-name, one-form allow-list — see F23. |
| F19 | MEDIUM | `7ba1825` + `a5af4b2` | **FIXED** (its cross-seat handoff is F23) | `SHADOW_CARD = { light: '0 3px 8px rgba(30,58,138,0.18)', dark: '0 4px 8px rgba(0,0,0,0.15)' } as const satisfies Record<ColorScheme, string>` read via `[scheme]` (`glass-tokens.ts:103-106`, `:155`); both shadow rulings written at source (`Colors.ts:376-378` quoted for nested; A44/A54 intent vs dead iOS shadow for card); pinned in `glass-card-recipe.test.tsx:611-631`. Dark byte-identical to before → no visual delta. Web accepts the refutation of its own MEDIUM at the phone's source. The author recorded probe Q7 (severed derivation) as SURVIVED and handed the coverage to F18's scan in writing — the handoff was not picked up (F23). |
| F20 | LOW | `700ce2b` | **FIXED** | `as const satisfies CSSProperties` on all three fragments; `as string` cast gone; `_f20` compile-time pin added — ts probe 6: reverting one fragment to the annotation → TS2322 at the pin. |
| F21 | LOW | `f1491b9` | **FIXED** | `readStop` closed with `\s*\]`; three ungated unit cases (2-tuple reads, 3-tuple and 1-tuple throw). sf checked the regression direction (phone's `] as const` and the CTA tuple both still match): 115/115. |
| F22 | LOW | `d65a2c9` | **FIXED** | `['header/sheet border', 'rgba(28,78,132,0.6)']`, `['header highlightTop', 'rgba(153,186,221,0.1)']` in `BANNED` with the one-literal-two-tiers note for U4.1. |

**Master carry `044578a` (integrator m.1–m.4):** one conflict (`rn-token-parity.test.ts`), unioned; F16's tier pins relocated into F11's ungated describe (verified in the diff — both membership `it`s sit under "the guard's local invariants"); F17 wins the stuck-reader hunk; `SCHEME_INVARIANT` has exactly one definition (the guard's) with a typed mirror on the expected side. 4/4 probes killed, three of them phone-ABSENT (`8 skipped` is the point). Fix-introduced regressions: none found by five lanes.

## New findings (append-only)

### F23 [MEDIUM] F18's scheme-half scan is a hand-typed allow-list of two identifiers and one spelling — F19's `SHADOW_CARD` (the third two-half record, created in this same round) and the bracket / destructure forms of all three walk past it; the PR #41 mapping claims the opposite
Lanes: typescript — original label: HIGH (DEMOTED, see Dropped / demoted); tests — original label: MEDIUM; silent-failures — original label: MEDIUM; type-design — original label: MEDIUM (F18 PARTIAL). Four lanes, one defect: the third recurrence of the hand-maintained-mirror class (W0 F2 → `PALETTE_KEYS`, W1 F16 → `TIER_KEYS`, now a regex).
File: `features/demo/ui/__tests__/glass-tokens.test.ts:113` — `SCHEME_HALF = /(?<!\btypeof\s+)\b(?:GLASS_TIER|palette)\s*\.\s*(?:dark|light)\b/`, asserted at `:244-249`; the uncovered record: `features/demo/ui/glass-tokens.ts:103-106` (`SHADOW_CARD`), `:155` (`shadowCard: SHADOW_CARD[scheme]`); the dropped handoff: `glass-card-recipe.test.tsx:623-629` ("`SHADOW_CARD` belongs in that scan's list. Not duplicated here — F18's owner holds that file.")
Issue: four independent SURVIVED probes on shipped code. ts probe 4 / tests D / sf: `shadowCard: SHADOW_CARD[scheme] → SHADOW_CARD.dark` — scan green, cold tsc 0, full suite 3,575 green. tests D2 / td: `GLASS_TIER['dark'].header` — SURVIVED; td: `const { dark: tier } = GLASS_TIER` — SURVIVED. Negative control (sf, ts 2a): the dot form on `GLASS_TIER` at the same line is KILLED — the only difference is the identifier / the accessor spelling. The PR #41 mapping's F19 row states "probe Q7 … covered by F18's scan" — it is not; the author's own handoff note says where the coverage should go and nobody held it across the branch boundary. AGGREGATOR PROBE (in-memory, tree untouched, 138 non-test files under `ui/` minus `SCHEME_DECLARERS`, comment-stripped): a name-agnostic pattern (`\b[A-Za-z_$][\w$]*\s*\.\s*(?:dark|light)\b`), a bracket pattern and a destructure pattern each produce **zero offenders on the clean tree** — no false red — and between them catch all three severed forms of `SHADOW_CARD`, while the shipped pattern misses all three. The widening costs nothing. Bracket/destructure are not live anywhere under `features/`, `app/`, `lib/` (tests, sf grepped) — the bracket form matters because the MANDATED idiom is `GLASS_TIER[scheme]`, so a developer hard-coding a half by copying the correct shape writes `GLASS_TIER['dark']` more naturally than `.dark` (td).
Fix: make the scan name-agnostic and form-agnostic in one `it`, keeping the exemption on the FILE (`SCHEME_DECLARERS`, which already works) rather than on the identifier: `/(?<!\btypeof\s+)\b[A-Za-z_$][\w$]*\s*(?:\.\s*|\[\s*['"])(?:dark|light)\b/` plus a destructure alternative `/\{[^}]*\b(?:dark|light)\b[^}]*\}\s*=\s*[A-Za-z_$][\w$]*/` (verified no false red on the clean tree). Keep the two SURVIVED forms as negative controls beside F18's existing kill. Add one line to `SHADOW_CARD`'s docblock pointing at the scan so the coverage claim is true where it is written. Re-run ts probe 4, tests D/D2 and td's destructure probe against it before pushing. If the owner prefers the enrol-yourself shape sf suggests (key off `satisfies Record<ColorScheme,`), it must still catch the three spellings — the pattern above is the floor.
Owner: `a9f135565ce43133b` (U1.4 seat — `glass-tokens.test.ts` owner; F18's author)

## Dropped / demoted lane findings

| Lane item | Lane · label | Disposition | Reason |
|---|---|---|---|
| Scan misses `SHADOW_CARD` — severing passes scan + tsc + suite | typescript · HIGH | DEMOTED → F23 MEDIUM | Same defect class and same use-day as F18, which was graded MEDIUM in r1 on the same facts (silent SURVIVED, nothing renders wrong today, plan §9 clause 12 schedules a scratch-worktree flip at U8 exit — that day has a check, and a black drop under white cards is visible in captures). "Passes scan + tsc + suite" was equally true of F18's two r1 probes. Three of four lanes graded MEDIUM; consistency with my own r1 precedent (#2: survivor only where the use-day has a scheduled check = MEDIUM) holds. The aggravating fact — a coverage CLAIM in the mapping that was never probed — is a pipeline finding, recorded below, not a severity multiplier on the code. |
| Bracket + destructure forms survive (F18 PARTIAL) | type-design · MEDIUM; tests · MEDIUM (2nd touch-point) | MERGED into F23 | Same hard-coded set on the same line: the alternation misses a NAME and the character class misses two SPELLINGS. One regex, one commit. |
| F19 handoff not picked up | silent-failures · MEDIUM | MERGED into F23 | Same defect from the process angle; the handoff text is F23's citation. |
| `UNANCHORED` is an untyped `string[]` | integrator m.4 (2) | not filed | A part named there that does not exist reds the `toEqual`; failure direction is loud. One line whenever a second unanchored part appears. |
| `INVARIANT` must be hand-sorted (`.sort()` on one side only) | type-design residual | not filed | Sorted today; a mistake fails immediately and legibly. |
| CLI still prints "✓ all 107 rows match" under a `TIER_KEYS` shrink | silent-failures bounded residual | not filed | Membership is necessarily test-only (`.mjs` cannot import TS); the ungated test runs everywhere. Stated in code at `check-rn-parity.mjs:263-268`. |

## Owner routing summary (targeted delta)

| Owner | Finding IDs | File |
|---|---|---|
| `a9f135565ce43133b` — U1.4 seat | F23 | `features/demo/ui/__tests__/glass-tokens.test.ts` (+ one docblock line in `glass-tokens.ts:103-106`, the U1.2/3 seat's file — declare it as a one-line cross-territory edit in the commit, or let the U1.2/3 seat land that line) |

## Lanes to resume for the targeted delta

The four probing lanes, on F23 only: typescript (re-run probes 4 + 5), tests (D, D2), silent-failures (the `SHADOW_CARD.dark` probe + its negative control), type-design (bracket + destructure). Web: no resume — F23 is a test-file regex with no rendered change.

## Ledger interaction

Triggers this diff satisfies: none. Trigger-lapsed findings: none. Rows written this round: **none** (F23 is a fix, not a deferral; §94–§98 stand unchanged).

## Agent IDs

Unchanged from `w1/VETTED-r1.md`. Integrator: `dt-integrator` (ID in the orchestrator's record).

## Pipeline notes

- **UNSETTLED — pending an external ruling, not a verdict mover:** U4.1 (W2) has MEASURED a contradiction with F14's fragment shape — a consumer writing `{...fragment, borderColor}` still loses the lit edge under the three-side-longhand form. That is the documented hazard (any four-side shorthand after the spread wipes a longhand top edge; F14's form is the RE-TINT idiom, not immunity to a forbidden override) — but if U4.1 needs a fragment that survives a consumer `borderColor`, the edge cannot live in `borderTopColor` at all (an inset `box-shadow` edge, as `glassWizardHeaderBar` already does, is the obvious alternative and moves the hazard to `boxShadow` overrides instead). The Fable partner is producing ONE ruling with a harness. F14 stands FIXED against its own finding; if the ruling changes the fragment shape, that is a W2 change to `glassCard`, not a W1 regression, and the F14 negative controls become its test bed.
- **The round's process defect, stated once:** F19's commit named the file it could not edit and the owner who should, and the mapping comment then asserted the coverage as done ("covered by F18's scan") without anyone probing it. A cross-seat handoff is PRESCRIPTION-UNVERIFIED until the receiving seat's commit lands — the mapping comment needs a column for "asks of another seat" so the integrator or aggregator can see an unclaimed one. Same shape as the W0 note about integrators citing their own prior proposals as ledger state.
- **Lane quality:** three fixes were strictly better than the prescriptions that asked for them, and the lanes said so with probes — F14 (web withdrew its own remedy; ts endorsed the refutation), F16 (the derived parts set caught the 5th-part shape no lane had probed), F18 (`SCHEME_DECLARERS` instead of `TOKEN_MODULES` — my r1 prescription and two lanes' would have left `glass-tokens.ts` unscanned). Type-design recorded F15 against its own W0 fix. Zero fix-introduced regressions; every r1 survivor re-run as the identical mutation and killed.
- **Integrator:** the F16-pin relocation into F11's ungated describe was the right non-textual union — applying F11 only to the pin F11 named would have shipped half of it. m.4 (1)'s rule ("membership pins go in the ungated describe") is already stated in that describe's header; no further action.
- **Orchestrator housekeeping:** two probe worktrees were on disk mid-round (`probe-u2.2-recipe`, `probe-w1d-tests`; the tests lane reports tearing the latter down at the end of its pass) — confirm `probe-u2.2-recipe` is live under a W2 seat or remove it with `tools/worktree-remove.ps1`. No W1 build log exists at the brief's path (`_captures/w1-assembly-gates.log.build`) in either round; the integrator's cold `pnpm build` (107 kB) is the record. No `after-fixed/` W1 captures yet — the verification seat's re-cut is the first pixel-level look at the fixed wave; no rendered value changed this round (`SHADOW_CARD.dark` byte-identical, F15/F20 type-level, the rest tests), so a re-cut at `044578a` is equivalent to one at the F23 head.
